# Orca-Kernel v0.1 开发执行手册与验收总清单（两组主对照版）

**版本：执行手册 1.1｜修订日期：2026-09-06｜沿用原稿研究日期：2026-09-06｜初始状态：0/11 关通过，全部待执行**

**用途：** 作为 Fork 内唯一的开发推进依据，建议保存为 `V01-TODO.md`。每次只放行一个主关卡；关卡内按子步骤执行。文档编写完成不代表环境、功能或实验已经通过。

**交付目标：** 基于 Orca 的小幅二开，让当前有效方法成为可关闭、可检查、可接管的协作模式。**主对照只保留两组：CURRENT（Codex 当前生效的一条内置提示词＋长期记忆＋触发或读取记忆的提示词钩子＋Orca 1.4.188 的基础设施与惯常操作）与 KERNEL（修改后的版本）。** 核心问题只有一个：新版本相对你现在的工作方式，是否有真实净收益。

**2026-09-06 用户决定，覆盖本手册冲突条款：** 本轮采用 Docker-first，取消独立 Windows 用户前置要求；Windows 日常 Orca 不变，实验 Orca/Codex、非 root HOME、记忆副本和目标仓库在容器内。VM 只作失败后的备选，不同时建设。后文保留 K 作为上述三项配置起点的记号，“Kit”不再表示独立压缩包或新系统。提示词钩子不默认是可执行拦截器。最新局部授权允许 Docker 有界检查及复用本地镜像的无宿主挂载小容器冒烟；完整 CURRENT 复现待确认留到 G04，不阻塞前置检查、源码阅读或未计分环境冒烟。额外下载、安装、系统变更和模型请求仍受原授权边界约束；不自动放行 G00/G03。

**本次修订：** 取消独立“原生 Orca 性能对照组”及其配置、运行台账和验收负担。需要原生能力参照时，直接使用 **CODEX（原生 Codex 独立运行，可选）**，不经 Orca、不加载 Kit/Kernel。CODEX 单独记录、默认不执行，不进入主对照样本和 v0.1 放行前提。主实验由原来的安排收缩为 **2 类任务 × 2 组 × 每组 3 次＝12 次**。

**仍然保留：** 未修改上游 U 的构建／启动／停止冒烟，用来识别环境或上游问题。它是功能回归的起点，**不是另一个比较组，不要求长期维护一套原生 Orca 环境**。本版替代旧手册中的实验分组、环境命名、运行台账及对应 Agent 指令；旧报告如已存在，只能作为历史资料，不混入新批次。

**明确不做：** 新 IDE、终端/进程管理器、任务数据库、消息系统、分布式调度、全自动恢复、通用安全沙箱、进化层、行业模板市场和生产自动发布。OpenDashboard 只承担有限真实任务验证，不同时重构整个产品。

> 原稿研究说明保留在附录 F；其中“未访问主机/未执行”描述原稿撰写时的状态。本项目随后已有 G00 只读证据，见唯一看板和交接，不以原稿初始状态覆盖实际进展。命令仍需在选定 U、实际 M 和真实操作系统上复核；正文功能规则是设计要求，不是已实现功能。

---

## 0. 怎样使用这份手册

### 0.1 你、协调 Agent、开发 Agent 分别做什么

| 角色 | 负责 | 不负责 |
|---|---|---|
| 你（产品与最终放行人） | 批准环境变更/付费执行/外部写入，确认需求与最终成果，放行下一关 | 手工搬运所有任务、替 Agent 无记录地修好结果 |
| 协调 Agent | 阅读当前关卡、检查入口条件、分配互斥范围、核对证据、更新唯一看板 | 越轨改业务代码，凭自述把关卡改成通过 |
| 开发 Agent | 在指定分支与 Worktree 实现最小改动、运行测试、提交证据 | 改上游基线、扩大范围、降低验收、创建第二套基础设施 |
| 验收执行者 | 对指定版本独立运行既定检查，记录原始结果 | 默默修改被验收产物再宣称原候选通过 |

这是职责划分，不要求新开四个常驻 Agent。初期可由一个协调会话与一两个长期开发轨承担。维修返回原轨；不因失败额外创造“加固队、复查队、发布队”。

### 0.2 三种标签，避免把提案当现有命令

- **【已核查上游】** ：已在本次官方资料/源码中看到，仍需在 U 复核。
- **【参考命令】** ：用于本地操作的示例，未在你主机执行。运行前填写真实参数，逐条检查结果。
- **【拟新增】** ：你需要在 Fork 中实现的最小能力。本文不会把它写成一个已经可执行的 `orca kernel ...` 命令。

Windows PowerShell 与 Bash 命令不能混用。`<真实 ID>` 之类只是说明占位符，不能原样执行。不要为了命令成功使用管理员权限、全局 `setx`、关闭全部权限检查或清空用户配置。

### 0.3 步骤图与唯一状态表

```text
G00 保护现状 → G01 冻结 Fork/M/K/U → G02 上游技术冒烟
                                       ↓
G03 全链路隔离验收 → G04 冻结 CURRENT 与两组规格
                                       ↓
G05 契约/开关 → G06 派发/交接/停止 → G07 验收/整合
                                       ↓
G08 回归门 → G09 CURRENT/KERNEL 两组对照 → G10 实验版评审/退出

任一关失败：留在本关，回原轨修复；不跳关、不额外造平台。
```

状态流：`待开始 → 执行中 → 待验收 → 通过`。失败或事实不明进入 `阻塞`；上游依赖发生变化则为 `需重验`。

| 关卡 | 核心产物 | 状态 | 负责人 | 提交/证据 | 放行人 |
|---|---|---|---|---|---|
| G00 | 现状与保护清单、隔离决定 | 待验收 | 当前协调 Agent（单轨） | [G00 接手记录](docs/G00-HANDOFF.md) | 用户（未放行） |
| G01 | Fork、当前 M/K 与上游 U | 待开始 | 待指定 | — | 用户 |
| G02 | 未修改源码与监督 Worker 冒烟 | 待开始 | 待指定 | — | 用户 |
| G03 | 实例、主目录、会话、CLI 隔离证明 | 待开始 | 待指定 | — | 用户 |
| G04 | CURRENT 冒烟与两组实验规格 | 待开始 | 待指定 | — | 用户 |
| G05 | 受管模式、契约与计划检查 | 待开始 | 待指定 | — | 用户 |
| G06 | 受约束派发、交接、停止 | 待开始 | 待指定 | — | 用户 |
| G07 | 实际候选验收与整合 | 待开始 | 待指定 | — | 用户 |
| G08 | 正常/故障回归与完整闭环 | 待开始 | 待指定 | — | 用户 |
| G09 | 冻结候选 C 与两组完整实验记录 | 待开始 | 待指定 | — | 用户 |
| G10 | 实验版决定、已知限制和退出方案 | 待开始 | 待指定 | — | 用户 |

“通过 3/11”不是“完成工作量 27%”。不使用缺乏依据的百分比。只由协调者维护本表；开发者在自己的交接中报告，不同时编辑看板。

**2026-09-06 接手记录：** 本文件从用户提供的两组对照版执行手册导入。初始声明与研究来源保留；本轮只补 G00 实际状态，仍为 0/11 关通过。实验身份尚未确认、实际 Kit 尚未完整定位，G00 不放行。事实、未知项及冷备份方案见上方交接；后续关卡均未开始。

**同日后续决定与检查：** 上段为首次接手历史。用户已确定 Docker-first 和 CURRENT 三项配置定义，取消寻找独立 Kit/Windows 实验账户的阻塞。本轮 Docker CLI/Compose 可用，但 Engine 管道不存在；提示词的精确身份与记忆导出/触发机制尚需核实。G00 继续阻塞，G01–G10 保持原状态；检查结果见同一 G00 交接的后续补充。

**同日最新确认：** 用户确认《Codex / Orca CLI 通用执行协议》就是当前使用的提示词；内容与系统/托管 home 的 AGENTS.md 一致，重复粘贴按单份保存，不重复注入。Docker Engine 已恢复可达，实测 Server 29.5.3、Linux/x86_64、32 CPU、约 15.3 GiB Engine 内存。以上两项阻塞已解除；长期记忆/提示词钩子的基线导出方式尚未确认，G00 仍阻塞，G03 仍未开始。旧错误作为历史保留；最新证据和提示词正文见同一交接。

**同日局部依赖修订（当前有效）：** 上段“记忆未导出使 G00 阻塞”不再生效。用户允许有限定位并把未知项留给 G04。Docker 实际命令全部有界执行成功，本地镜像的非 root、无网络/无挂载小容器已退出 0 并仅删除自身；G00 提交待验收，仍为 0/11 关通过。固定 Orca serve 尚未运行：授权目录没有可复用 Docker 配置，本地未发现 Orca 镜像；下一步需要最小配置准备及固定 Linux 发行包下载授权，不是记忆或 Docker 引擎失败。

### 0.4 每关统一放行规则

进入前：上一关已通过，基线与权限未失效。执行后：实际命令、实际候选和实际结果可对应。没有执行的测试写“未测”，不能写“预计通过”。

开发 Agent 只提交“待验收”或“阻塞”。用户批准后协调者更新看板；本关授权只覆盖列出的操作。已有明确授权不重复询问，但新建外部资源、扩大写权、额外费用、破坏性恢复须单独确认。

运行事实仍在 Orca。看板只是项目验收索引，不是第二套运行状态库。每关只保留一个短交接，详细日志引用已有文件即可。

---

## 1. 本次源码复核后，必须修正的认识

### 1.1 配置目录隔离不等于 Codex 全部隔离

【已核查上游】开发版可通过 `ORCA_DEV_USER_DATA_PATH` 指定应用数据；不指定时会落到默认开发目录。CLI 包装还会把该值用于 `ORCA_USER_DATA_PATH`。这能帮助隔开 Orca 状态与控制端点。[S03][S04]

**但是，Codex 相关源码还会从实际系统用户的 `homedir()/.codex` 取得全局资源，涉及 AGENTS.md、提示词、skills、hooks 等；还存在配置镜像/设置回写和会话桥接/回填路径。独立 userData 或单独设置 CODEX_HOME，都不能独自证明没有继承旧 Kit 或旧会话。** 具体采用了哪条路径，必须在 U 的调用链和真实 Worker 中验证。[S10–S13]

因此，本轮采用**容器内非 root 用户、独立 HOME 与状态副本，并验证可恢复的试验起点**。不要求创建或切换 Windows 用户。不得将 DW 的整个 HOME、`.codex` 或日常 Orca 数据挂入实验容器；容器边界需经 G03 实测，不能仅凭容器存在宣称隔离。

### 1.2 同一 checkout 的不同 profile 必须串行

【沿用原稿核查】开发 CLI 包装不只是读状态，还会生成绑定当前 profile 的包装文件；目标包括 checkout 内的 `out/bin` 和 profile 自己的 CLI 目录。即使只是换 profile 查询，也可能改写同一 checkout 的公共包装。[S04][S05]

**BASE-SMOKE、KERNEL-dev、KERNEL 正式运行复用同一 Fork checkout 时，必须顺序启动并核对 CLI。** 不允许在同一 checkout 下同时运行不同 profile。CURRENT 使用已冻结的现有程序／工作流；若其实际运行方式也是源码开发版，就保留它自己的原有 checkout，不强行改成新方案。正式性能对照统一串行，不互抢 CPU、磁盘和账户限流。

### 1.3 “窗口开着”“ok:true”“任务 completed”都不是完整验收

`status` 即使运行实例不在线，也可能以 `ok:true` 返回“查询成功”，实际运行状态在 `result.runtime.reachable/state`。必须看真实状态和 runtimeId。[S07]

Worker 报告完成会进入原生生命周期，但不替代你的候选验收。[S20] 下一任务还必须实际取得已经验收的依赖代码；单独在任务表里标完成不会自动让代码出现在其工作区。

### 1.4 三类隐藏副作用要提前审查

- `build:cli` 会调用开发 CLI 安装脚本；某些平台会尝试创建全局别名。使用 checkout 内显式包装，不授权全局提权覆盖。[S02][S06]
- Node 测试与 Electron 开发入口会准备不同原生模块运行环境。不要在同一份依赖目录上，一边跑活跃应用，一边执行会重建模块的测试。[S02][S08]
- Orca 仓库自己的 setup 含安装步骤，并可能调用 `ORCA_INTERNAL_DEV_SETUP` 指定的本地程序。先阅读 setup 和实际环境，不把“开 Worktree”理解成纯文件复制。[S23][S24]

---

## 2. 隔离架构：先选一种经过验证的方式，再开工

### 2.1 日常环境、两个主对照和一个可选参照

| 身份 | 来源 | 是否修改 | 实验地位 |
|---|---|---|---|
| D 日常环境 | 你现在正在使用的安装／源码版及配置 | 不直接修改其状态，不在其中做危险实验 | 被保护的真实日常环境，不额外重复计数 |
| CURRENT 当前方案 | 在实验容器复现 Orca 1.4.188＋当前生效的一条内置提示词、长期记忆及读取/触发记忆的提示词钩子＋惯常操作 | 保存经确认的起点，不顺手优化；Windows→Linux/无头适配单列 | **必做主对照：现状基线** |
| KERNEL 新版本 | Fork 从 U 派生，G05–G08 修改，G09 冻结候选 C | 允许本版最小改动；正式批次冻结 | **必做主对照：改进方案** |
| CODEX 原生 Codex | 在干净环境直接运行原生 Codex | 不加载 Orca、Kit、Kernel；不人为削弱原生正常能力 | 可选、单列，不是主对照与放行前提 |

**版本记号：** M＝Orca 1.4.188 的实际构建与运行方式（记录宿主 Windows 和容器 Linux/无头差异）；K＝经确认的内置提示词、长期记忆与提示词钩子起点；U＝Fork 的上游起点；C＝Kernel 候选提交；P＝业务目标仓库起点。K 不要求独立 Kit.zip、脚本包或另一个系统。C 是提交记号，不是实验组名。

**CURRENT 必须忠实于“我现在用的”。** 记录 M、三项配置的真实加载来源/触发机制、模型设置和惯常操作。保留通用协作经验；去除秘密、认证、本轮答案及不应跨轮继承的会话残留。每轮从经确认起点取得独立副本，允许当前流程在本轮正常更新记忆，但不自动传播到下一轮。不得升级 Orca 或重写提示词来迁就实验；不能把普通提示词约定冒充硬拦截。来源/导出不明即记录未知，不编造路径，不复制整个日常数据目录。

U 优先与 M 对应的源码基线一致或尽可能接近。若 M 的精确源码不可得、两边版本不同或运行方式无法完全一致，仍可比较“当前整套方案 vs 新版本”的实际使用效果，但必须列出差异，不能把所有增益单独归因于 Kernel。不要为追求相同 U 反过来修改真实 CURRENT；也不因此增加第三套对照。

**工具源码仓库只要求一个 Fork `orca-kernel`。** G02 在其尚未修改功能时对 U 做临时 BASE-SMOKE；保存结果后继续开发，不长期维护独立原生性能环境。需要复查上游回归时，可在干净临时 worktree 中重现 U，但不要切换／覆盖有未提交工作的开发目录，也不把它加入性能台账。CURRENT 优先使用实际已在用的发行包或原有源码运行方式；不强制再建一份“原生＋Kit”的新实现。

**被开发的业务目标仓库** 是每轮任务的独立副本，与工具 Fork 分开。开发 Fork 使用稳定工具；未经验证的 Kernel 不能成为开发自己的唯一运行和恢复手段。

建议在实验容器非 root 用户的独立 HOME 下使用（实际路径以容器配置与运行结果核验；不是宿主 DW 目录）：

```text
OrcaLab/
  code/orca-kernel/             唯一必需的工具 Fork；G02 先在 U 冒烟
  baseline/current/            M/K 的脱敏清单；必要发行包仅本地保存
  profiles/base-smoke/          临时上游功能验证，不参加计分
  profiles/CURRENT-smoke/       现用方案复现；真实路径按该版本能力核验
  profiles/KERNEL-dev/          开发用，不复用为正式实验起点
  profiles/CURRENT-T1-r1/ ...   每轮独立，不能复制已运行状态
  profiles/KERNEL-T1-r1/ ...
  targets/CURRENT-T1-r1/ ...    每轮业务目标独立 clone
  targets/KERNEL-T1-r1/ ...
  evidence/                    私有日志／备份，不自动上传
  # 只有启用可选参照时，才增加 CODEX 的独立配置和目标目录。
```

目录名称不是给旧版本强加新 profile 能力。CURRENT 在容器独立 HOME 内按 1.4.188 的真实入口复现；无法忠实复现时记录具体缺口，再提议 VM，不回退连接 D。不要共享可写 node_modules、构建输出、测试数据库和端口。依赖缓存可以复用，但不得夹带答案，冷热条件一致。

CODEX 仅在你明确批准时运行；它不要求新建任何 Orca 实例。其模型、版本、权限和原生子 Agent 设置在启动前记录，既不自动禁用正常原生功能，也不外接你的多 Agent Kit。

### 2.2 环境选择（用户已决定 Docker-first）

**R4 Docker：本轮唯一主路线。** 宿主 Windows 日常 Orca 保持原状。实验 Orca、Codex、独立 HOME、记忆副本和目标仓库在容器内，以非 root 用户运行；用户不切换 Windows 登录。G00 只读核对 Docker Desktop/Engine/Compose、当前 context 与容器模式、WSL/虚拟化、资源和代理。缺安装、需管理员/系统功能/重启时列出事实，不自行实施。

G00 放行后按 G01–G04 逐步准备最小 Dockerfile、Compose 和启动说明。先核对 v1.4.188 对应资料及选定 U，CURRENT 不能使用 latest。初期一套 Orca 及其 Worker 在一个实验容器内，复用 Orca Worktree；不创建每 Agent 一容器的调度器。两组使用相同容器运行方式和资源，串行试验。

**R2 VM：仅作备选。** 仅在普通权限下无法运行或无法忠实复现 CURRENT 时，提交准确错误和已尝试的有限修复，再提议切换。未经批准不安装 VM 栈，不同时建设两套环境。

**R1 独立 Windows 用户：不再是前置要求。** 历史 R1 方案保留为背景，当前不创建、不要求用户切换。容器内非 root 用户与独立 HOME 是本轮要求。

**R3 日常用户多 profile：仅用于受限只读观察。** 它不替代实验容器，不在日常 profile 启动未经验证的实验 Codex。WSL 是 Docker 后端条件，不等于自动切断宿主磁盘与互操作通道。

### 2.3 基线与每轮复位

保存经确认的三项起点：一条实际生效的内置提示词、保留通用经验的长期记忆、触发/读取记忆的提示词钩子。三者分别注明真实来源和保存方法；无法导出或确认加载时列为未知。不得将普通 AGENTS.md、模型内置指令或状态 hooks.json 未经核对就互相替代。

本项目所称“内置提示词”现已由用户确认为《Codex / Orca CLI 通用执行协议》，以交接中保存的单份正文为起点，与已核对的 AGENTS.md 相符。用户名称沿用，但不把它替换成 models_cache.json 中的模型厂商指令，不据此宣称长期记忆钩子也已完整导出。

每轮使用独立实验 HOME、Orca 状态、记忆副本和目标 clone。允许 CURRENT 原有流程在本轮正常更新记忆；下轮回到经确认的起点，不自动继承本轮答案、会话或经验增量。CURRENT/KERNEL 的通用经验、组别差异及更新权限在 G04 明确。不可导出的提示词/记忆机制不能用新建 Kit 假装等价。

禁止挂载或复制整个宿主 HOME、DW/.codex、日常 Orca 数据、日常业务仓库或 Docker socket。只按经确认的清单导出必要配置/通用经验到私有起点；本轮未执行导出。秘密和认证不得进入 Git、镜像层或公开报告，按正常认证流程在运行时取得。

复位仅处理已识别且授权的本项目容器/卷/目录；先停止并确认无活跃写入者，保留本轮证据，再使用干净起点。不得全局 prune、删除未知卷或复制已运行 profile。容器停止/重建不证明外部卷已复位，必须在 G03 用跨轮标记验证。

禁止默认 privileged、关闭 sandbox 或开放公网控制口；宿主如需访问，只绑定经批准的 loopback 端口，另核对容器内实际监听。不能用 pairing-address 代替绑定范围验证。正常权限失败时保留证据，有限修复后再提 VM，不持续扩大容器工程。

---

## G00｜只读盘点、保护日常环境、选择隔离路线

### 目标与入口

不要求已有 Fork。目标是拿到本机事实。按用户本轮局部授权执行 Docker 路径、version、context show/ls、格式化 info、Compose 的有界检查；确认本机引擎后，可复用已有镜像做无宿主挂载、非 root 的小容器冒烟。只更新现有手册与记录；额外下载、安装、提权、重启、影响现有容器的切换、代理修改和模型请求超出已有授权时，提出具体动作，不泛泛重选环境。

### 你要做

确认你日常使用的是哪一个 Orca 窗口/安装包；告诉 Agent 哪些目录不能触碰。已选择 Docker-first，不要求 Windows 账户切换。先确认 Engine 可用；需要系统变更时列出准确人工动作，不能立即停止日常任务或复制日常配置。

### Agent To-Do

- [x] **G00.01** 识别真实 OS、架构、shell，区分 Windows App、Git Bash、WSL 和远端运行。没有证据不假定是纯 Windows。
- [x] **G00.02** 只查询 Orca/Node/Git/pnpm/Codex 命令的实际解析位置、已知版本；未知命令先查路径，不贸然启动。
- [x] **G00.03** 有边界核对实际 Codex 版本、系统 home、CODEX_HOME、Orca 托管 home、已知用户/项目规则、memories、hooks 和插件引用，保存实得来源和未知项。原始协议已保存，加载/导出未验证部分单列 CURRENT 复现待确认，留给 G04；不寻找 Kit.zip、不全盘扫描或复制整个 .codex。此勾选仅表示有限定位已执行。
- [x] **G00.04** 记录与路由、代理、setup 有关的环境变量是否存在，只显示名称。特殊关注继承的 ORCA 环境、CODEX_HOME、HOME/USERPROFILE、WSL、ORCA_INTERNAL_DEV_SETUP。
- [x] **G00.05** 对用户指定的日常仓库只读检查未提交修改；记录分支与状态，不搜遍整个磁盘，不读业务秘密。
- [x] **G00.06** 只读检查 Docker Desktop/CLI/Engine/Compose、context/实际容器模式、WSL/虚拟化、资源、代理、后台 Agent 与模型/费用边界。历史 Engine 不可达已解除，最新 Engine 结果和容器内未知项明确登记；预算未批准。此勾选表示前置检查已执行，不表示 G00 或 G03 通过。
- [x] **G00.07** 提出一份冷备份方案：停止的是哪些已确认进程、复制哪些状态、放在哪里、何时恢复验证。运行中数据库普通复制不直接当有效备份；配置快照不一定包含系统钥匙串。
- [x] **G00.08** 记录用户已选择 Docker-first，提交具体阻塞和未知项；必要基线保存/备份在确认范围后执行，恢复验证限实验副本，不覆盖 D。此勾选不代表 G03 隔离通过。

### 参考命令：只读定位

PowerShell（不要全量输出环境变量值或进程命令行）：

```powershell
$PSVersionTable.PSVersion
[System.Runtime.InteropServices.RuntimeInformation]::OSDescription
[System.Runtime.InteropServices.RuntimeInformation]::OSArchitecture
Get-Command orca,orca-dev,orca-ide,node,pnpm,git,codex -All -ErrorAction SilentlyContinue |
  Select-Object Name,CommandType,Source
Get-ChildItem Env: | Where-Object Name -Match '^(ORCA|CODEX|WSL|HOME|USERPROFILE)' |
  Select-Object Name
# 只对明确授权仓库使用：
# git -C '实际仓库绝对路径' status --short --branch
```

Bash：

```bash
uname -srm
for cmd in orca orca-dev orca-ide node pnpm git codex; do
  type -a "$cmd" 2>/dev/null || true
done
# 仅变量名称，不输出值：
env | cut -d= -f1 | grep -E '^(ORCA|CODEX|WSL|HOME|USER)' || true
# git -C '/已授权仓库' status --short --branch
```

Linux 外部 shell 的 `orca` 可能是读屏软件，先定位，不直接执行。[S09] G00 不调用尚未审查的 checkout 包装，因为它会生成文件。[S04][S05]

### 产物与通过标准

产物只需一张环境表：D 版本/位置、实际 home、保护范围、CURRENT 三项配置来源、Docker 前置检查、备份/导出状态、未知项。保存在本手册或一个脱敏交接中。

通过要求：日常环境、实验路线和保护范围明确；Docker 真实状态有命令、退出码及有界等待证据；已保存原始协议，CURRENT 的来源/导出未知项明确交给 G04。记忆未完整定位不再阻塞 Docker 前置检查、源码阅读或未计分环境冒烟。实际权限或环境失败按首个失败层记录；缺额外下载/安装授权时列准确动作，不自动执行。总控只提交待验收/阻塞，由用户放行。

### 失败处理

保持 D 原状。只补事实，不改全局 PATH，不全局更新 Node，不清空登录状态。备份无法验证时可以继续纯源码阅读，但不能将涉及日常状态写入的步骤放行。

### 给 Agent 的指令

```text
执行 G00 及本轮明确授权的有限环境冒烟；先读角色边界和最新依赖修订。
Docker 每条命令有界等待；确认本机目标后才运行获准的无宿主挂载小容器。
不要额外下载、安装、创建 Fork、改全局环境、启动收费任务或打印凭证。
输出真实 OS/运行方式、Docker 前置检查、CURRENT 三项配置来源、保护范围和未知项。Docker-first 已确定，不寻找 Kit.zip，不要求 Windows 换用户。
需要备份时先说明范围与副作用，获得授权后只在已批准位置操作。
提交“G00 待验收/阻塞”，不要宣布后续关卡完成。
```

---

## G01｜创建 Fork，冻结当前 M/K 与上游 U，审查副作用

### 入口与人工动作

G00 通过后按附录 B 准备 Docker-first 最小配置，核对 1.4.188 资料及所选 U。容器内使用非 root 实验身份；Fork 创建、名称与可见性按原授权规则确认。私有提示词/记忆起点、业务样例、认证与原始日志不放公开 Fork 或镜像层。

CURRENT 记忆/提示词钩子不能完整定位时标“CURRENT 复现待确认”，交给 G04；不阻塞已授权源码阅读或技术环境准备。优先复用已存在且审查过的 Docker 配置；没有配置就如实记录，不能声称已复用。显式加载已保存协议的临时环境仅是技术复现，不是完整 CURRENT。

### Agent To-Do

- [ ] **G01.01** 审查容器运行用户、独立 HOME/Codex home、挂载和端口；G02 运行后实测 UID/路径。不创建 Windows 实验用户，不用配置文件代替实测。
- [ ] **G01.02** 创建一个保留上游历史的 Fork；origin 指向你的仓库，upstream 指向 stablyai/orca。取消额外“原生对照仓库”的强制建设。
- [ ] **G01.03** 固定 CURRENT 的 M=Orca 1.4.188：核对同版本 Linux 发行包/源码、下载来源、实际执行器与启动方式，登记相对 Windows 日常方式的差异。不可得写未知，不用 latest 或最新 U 替换 M。
- [ ] **G01.04** 明确选择 U，优先对应 M；从 U 建立 Kernel 分支，保留起点记录。此时不改功能，G02 就在这份 Fork 上验证 U。M/U 差异预先登记。
- [ ] **G01.05** 保留已保存原始协议，记录记忆与提示词钩子的实得来源和可用内容，不编造导出。未定位/无法等价导出的部分登记 CURRENT 复现待确认，交给 G04，不阻塞源码或未计分环境冒烟。保留通用经验，秘密不进 Git/镜像；不复制整个日常目录、不擅自重写协议。
- [ ] **G01.06** 阅读 AGENTS.md、贡献指南、package.json、setup、开发启动与 CLI 安装脚本，再安装依赖。记录外部命令和可能写入位置。
- [ ] **G01.07** 审查 Actions、自动更新、遥测、app identity、URI scheme。初期只做源码版，不触发 installer/release，不覆盖 D。
- [ ] **G01.08** 输出一张复用／新增差异表及 CURRENT 复现方法。默认沿用基础设施；不再准备第三套原生 Orca 性能环境。
- [ ] **G01.09** 本批不自动合并新上游。换 U/M/K 时重新核对相关 G02–G04 及实验规格，旧结果分批保存。

### Fork 与容器准备约定（G00 放行后才执行）

G01 先核对 v1.4.188 的固定资料、源码提交和所选 U，审查构建/setup/全局 CLI/发布副作用，再在获准位置准备最小 Dockerfile、Compose 与启动说明。镜像内工具依赖可在构建阶段安装，Orca/Codex 运行必须切换到非 root 用户；不把认证、日常配置或记忆原始资料放入构建上下文或镜像层。

Fork 名称、可见性、外部创建和 push 按原放行规则确认；保留当前规划仓库及其历史，不覆盖目录、不 force push、不 mirror。只建立一个保留上游历史的 Kernel Fork，origin 指向获准仓库，upstream 指向 stablyai/orca；U 使用明确完整 SHA，不能把 latest/main 自动当作 M。

容器使用独立 HOME、项目源码与目标 clone；后续 Git 命令在实际容器目录内逐条执行并检查退出码。宿主仅进行已批准的容器控制，不运行原 Windows 实验用户模板来复制 DW 配置。G01 准备文件并不证明构建、启动或 Worker 成功，这些留给 G02。

### 阅读入口与通过标准

阅读 [S01][S02][S06][S23][S24][S26]。通过要求：M/K 与 U 可定位或未知项有明确记录；Fork 功能尚未更改；来源保留；实验资料无秘密；发布未意外启用。

**不为统一版本重写 CURRENT。** 若 M 与 U 不同，先确认差异和结论范围；必要兼容修复单独记名，不与未经修改的现状混淆。无法忠实复现 CURRENT 时先处理安装／环境，不拿无法运行的现状给 Kernel 垫底。

### 给 Agent 的指令

```text
只执行 G01；G00 未放行就停止。只建立一个 Kernel Fork，保留 history 和 origin/upstream。
记录当前真实 M/K，选择上游 U；G02 在未改功能的 Fork 上做临时冒烟。
不要另建原生 Orca 性能对照环境，不升级现用方案或顺手优化 Kit。
先读构建/setup/全局 CLI/发布副作用；输出版本、差异、复现方法，提交待验收。
未知版本如实记录；不要把源码 main 或旧厚版设计文档当成现用事实。
```

---

## G02｜不改功能，完成上游 U 的临时冒烟（不计入比较）

### 入口与人工动作

G01 通过。先在尚未加入 Kernel 功能的 Fork 上验证 U，使用临时 base-smoke profile。**这不是 CURRENT 组，也不是第三个对照组；它只建立环境／上游功能起点。** 必须在实验容器非 root HOME 内运行，你批准构建/安装与小额模型冒烟；正常认证，确认没有日常目录或 Docker socket 挂载。[S14]

本关不要求完整 CURRENT 记忆/钩子作为入口条件。用户明确授权的首个小容器/固定版本 serve 技术冒烟可先执行且不计分；模型请求仍须预算授权。它只建立环境事实，不能代替本关未修改 U、监督 Worker 与停止的完整验收，也不能代替 G04 的 CURRENT 复现。

### Agent To-Do

- [ ] **G02.01** 从 U 的 package.json 读取 Node/pnpm/脚本要求。此次观察的 main 为 Node 24、pnpm 12，但最终以 U 为准，不对全局工具盲目升级。[S02]
- [ ] **G02.02** 仅在容器实验源码目录安装锁定依赖。先查 setup 和安装脚本，再运行；记录 lockfile 是否意外变化。
- [ ] **G02.03** 创建 base-smoke profile；从干净终端显式绑定启动路径和 CLI。继承的远程选择/身份环境不能被当成当前实例授权。
- [ ] **G02.04** 构建 CLI，检查全局别名副作用。使用 checkout 内包装，不依赖全局 orca-dev。
- [ ] **G02.05** 启动未修改源码；Agent 自动启动时用上游后台启动约定，避免抢占用户窗口。[S01]
- [ ] **G02.06** 从外部操作终端读取实际状态，核对 reachable、runtimeId、进程和本组 profile；`ok:true` 本身不作为通过。
- [ ] **G02.07** 在仅本地、一次性的 scratch Git 仓库注册项目；查看/禁用不必要默认终端与 setup，避免创建即执行未知命令。
- [ ] **G02.08** 在 Orca 识别的协调 Agent 终端内创建 Run/Task，再用监督 worker-start 创建独立 Worktree；读取真正的 Dispatch 和 Worker 记录。
- [ ] **G02.09** 让 Worker 完成只读小任务，按注入的真实身份报告；验证读取结果和完成后释放。
- [ ] **G02.10** 对另一条明确批准的短任务测试停止；保留工作区和结果，记录真实仍存活/未知资源，不把停止当删除。
- [ ] **G02.11** 关闭该源码应用后运行相关原生检查，再启动时由 dev 入口恢复 Electron 原生依赖；同一 checkout 不边运行边重建 ABI。

### 启动约定（G02 与明确授权的未计分技术冒烟）

先审查 G01 已准备的最小 Dockerfile/Compose，核对固定版本、非 root UID、独立 HOME、挂载/卷清单、loopback 发布、资源和代理。构建与启动命令必须来自实际配置，不使用文档占位命令冒充已运行。不要为构建成功放宽 sandbox 或挂 Docker socket。

在容器内从未修改 U 的 package.json 读取 Node/pnpm 与锁定构建命令；通过原有开发入口或经验证的 headless 入口运行 BASE-SMOKE。后者若只能运行发行包，不能拿 M 的成功替代未修改 U 的源码冒烟。CURRENT 独立采用固定 1.4.188 的真实可用入口，禁止 releases/latest。

记录容器内实际程序、UID、HOME、Codex home、profile、CLI 与 runtimeId。同一 checkout 的不同 profile 串行；运行中不重建共享 native ABI。CLI 使用该容器自身解析到且核对过的显式入口，status 必须 runtime.reachable=true，app/runtime/进程相互对应，不回退连接日常 Orca。

首次按顺序验证：构建/启动 → Codex 配置 → 一条监督 Worker → 停止；G03 再验证隔离和复位。保留实际命令、退出状态与脱敏记录；没有执行的步骤仍为未测。

依赖修订：Docker 小容器与固定版本 Orca serve 的首个技术冒烟不需要长期记忆，不接生产仓库、不发未授权模型请求。只有环境/基础启动成功不代表 G02 整关通过；正式 G02 仍需其原有 U 源码和监督/停止证据。显式加载通用协议的临时复现不得记为完整 CURRENT 正式结果。找不到可复用配置、镜像或发行包时分别记录，额外下载按准确对象请求授权。

### 监督冒烟怎么做

**外部 PowerShell 能查状态，不代表它有权伪装协调者创建监督派发。** 创建 Run 和 Worker 的命令应在该 Orca 识别的协调 Agent 终端中执行；使用其实际注入的 CLI 命令，不复制 token，不伪造 `--from`。这是监督任务，不是“交出去后不用管”的完整 handoff。[S09][S14]

在该协调终端中，先从本地 U 的 skill/help 确认参数。参考顺序为：创建 Run、创建只读 Task、从回执取得实际 task ID、以 `worker-start --worktree new-top-level` 建立独立执行，再查询 Dispatch。不要照抄上游示例中的 `--worktree current` 来实现独立写入，因为 current 意味着共享当前 checkout。

```text
[使用已确认的 CLI] orchestration run-create --objective "G02 只读监督冒烟" --json
[使用已确认的 CLI] orchestration task-create --spec "只读 scratch README，报告内容概要，不修改文件，不启动子任务" --json
[使用已确认的 CLI] orchestration worker-start --task 实际taskID --worktree new-top-level --name g02-smoke --agent codex --setup skip --json
[使用已确认的 CLI] orchestration worker-show --dispatch 实际dispatchID --json
[使用已确认的 CLI] orchestration worker-read --dispatch 实际dispatchID --limit 50 --json
```

这里 `--setup skip` 仅因为 scratch 无构建依赖且已审查；真实项目不要默认跳过。`ready` 也不保证 setup 已结束，实际 U 支持的 wait-for-setup 条件需检查。[S17][S18]

等待消息、确认完成、停止/释放的细节集中在附录 A；不要从本文拼出固定“失败就强停、reset、重开”的脚本。

### 验收与失败处理

必须取得：未修改 U 的构建结果、真实 runtime 身份、一条有原生 Task/Dispatch 的成功监督记录、一条停止记录、已知基线失败列表。

窗口能开、fake transport 通过、Worker 自称有 Dispatch 都不算。关键链路失败不得豁免；无关且可复现的上游测试失败可以登记，后面比较是否新增。

失败时优先查版本/代理/原生模块/setup/认证与权限，不更换整个工具链、不 `--ignore-scripts` 掩盖安装失败、不禁用安全设置来冲绿灯。

### 给 Agent 的指令

```text
只执行 G02，在非 root 实验容器的 Kernel Fork 中使用尚未改功能的 U；这只是技术冒烟。
先按 U 的 AGENTS 与 package.json 安装构建，显式 profile 和 checkout CLI。
执行一次受监督的只读 scratch 任务和一次明确授权的停止试验。
记录实际 runtime、Task/Dispatch、setup 与残留资源；不伪造身份，不使用日常 profile。
相关测试与运行中的 Electron 串行；失败记录为事实，不改内核来修原生冒烟。
保存结果后提交 G02 待验收；不建立原生性能对照组，不把冒烟计入正式 12 次。G03 再验隔离。
```

---

## G03｜全链路隔离验收：没过这一关，不开发 Kernel

### 入口与人工动作

G02 已在非 root 实验容器通过。你允许在实验配置和记忆副本放置非敏感标记，并核对日常设置未受影响。本关实测 Docker 挂载、HOME、回写和复位；不修改 Windows 账户/权限，不创建备选 VM。

### Agent To-Do

- [ ] **G03.01** 记录 D、临时 base-smoke、CURRENT-smoke、KERNEL-dev 的宿主/容器身份、UID、实际 HOME/Codex home、程序来源、profile、CLI、挂载、卷与端口。只有 CURRENT/KERNEL 参加主比较；此时 Kernel 尚无功能修改。
- [ ] **G03.02** 逐一读取 runtimeId/进程/可达性，核对实际目标。不同 profile 不是同一个 runtimeId 的别名。
- [ ] **G03.03** 在实验版注册 scratch、改一个普通设置、添加一个非敏感标记；核对 D 对应仓库列表和设置没有改变，不要求 D 的运行日志绝对不变。
- [ ] **G03.04** 从外部控制终端、协调终端、Worker 终端分别查询身份。WSL Worker 必须用其实际注入的命令，不能替换成 Linux PATH 中同名程序。
- [ ] **G03.05** 在 U 定位 Codex home/资源镜像/会话桥接/设置回写调用链；检查实际 Worker home 与符号链接、junction、复制来源。只记录路径和摘要，不读认证内容。
- [ ] **G03.06** 验证 CURRENT 实际加载经确认的内置提示词、长期记忆副本及提示词钩子，并保留现有必要配置与惯常操作。核对提示词触发与命令 hooks 的区别。KERNEL 不重复注入 CURRENT 专有流程；通用经验与组别差异明确记录。BASE-SMOKE 不注入 K。
- [ ] **G03.07** 在容器内用无害配置/记忆标记及短会话测试传播；核对宿主日常 home 未被回写。允许本轮正常记忆更新，检查更新只在本轮副本；按支持的保存/导出方法处理运行中数据库，不复制活跃 SQLite 充当一致快照。
- [ ] **G03.08** 验证 Worker 目标路径在本轮授权目标内；独立 clone 不存在共享 alternates、指回日常仓库的链接或错误 push remote。
- [ ] **G03.09** 退出实验实例后，用绑定同一 profile 的 status 查询，必须不可达且不回退到 D。不要用会重新启动应用的 open 命令来做此负例。
- [ ] **G03.10** 重新启动仍恢复自己的状态；D 可继续使用。BASE-SMOKE、KERNEL-dev 和正式 Kernel profile 共用 checkout 时只串行切换；CURRENT 保持独立真实启动入口。
- [ ] **G03.11** 模拟“上一轮结束→新一轮”：恢复经确认的提示词/记忆/钩子起点，新建独立 HOME/状态/目标副本；旧答案、会话和本轮记忆增量不得自动继承，通用经验起点仍保留。仅处理授权项目资源，不 prune、不删未知卷。
- [ ] **G03.12** 输出四层结论：应用状态、CLI 路由、Agent 配置/历史、文件访问边界。每层写“已验证/有限保证/未支持”，不以总括的“已隔离”掩盖缺口。

### 只读检查链接示例

仅在已确认的实验 managed-home 中检查；不是查找或复制认证文件：

```powershell
# $ManagedHome 来自真实启动记录，而非猜测。
Get-Item (Join-Path $ManagedHome 'AGENTS.md') -Force -ErrorAction SilentlyContinue |
  Select-Object FullName,LinkType,Target
Get-ChildItem $ManagedHome -Force |
  Where-Object Name -In @('skills','hooks','prompts','plugins','sessions') |
  Select-Object Name,LinkType,Target
```

Bash 可对这些具体路径使用 `ls -ld`、`readlink` 和只读摘要。不要递归打开全部会话，也不要输出用户原始 AGENTS 内容到公共报告。

### 必须通过的隔离表

| 检查点 | 通过条件 | 不合格时 |
|---|---|---|
| runtime 路由 | 绑定实例、实际进程、profile 对应 | 停止，修 CLI/profile |
| Worker 配置 | 三项来源可说明，组别指令没有串线 | 修正容器配置/副本；不能忠实复现时提议 VM |
| 会话与回写 | 新运行不继承旧答案，修改不写向 D | 不能仅换 CODEX_HOME，核查真实调用链 |
| 文件访问 | 没有无意共享日常可写目录 | 移除共享或收窄权限，验证后继续 |
| 关闭负例 | CLI 不回退到其他运行实例 | 记录错误，不继续派发 |
| 重复运行 | 重置后起点可比且没有旧状态 | 修复 reset 流程，不删日常状态 |

配置、非 root 用户、容器挂载/网络边界与复位必须分别验证；Docker-first 的选择不构成已完成的隔离认证。涉及不可信仓库、安装脚本或更高风险代码时另设安全范围，不能利用“内测”略过。

### 失败处理与 Agent 指令

```text
只执行 G03。重点复查真实 homedir/.codex、镜像/桥接/回写，而非只看 userData。
按十二个子步骤留下脱敏证据；不打印 bootstrap token、登录文件或完整环境。
同一源码 checkout 不并行切换 profile。只能在实验配置里放置无害标记。
任何层无法证明隔离，就标阻塞，提交容器错误和有限修复结果；必要时提议 VM，不改日常配置、不持续扩大容器工程。
最后给出分层结论和重置方式，等待用户放行，不开始 Kernel 功能。
```

---

## G04｜冻结两组规格，忠实复现你当前使用的方案

### 目标与人工动作

G03 通过。你批准有限目标、预算、实验远端 push 和正常人工帮助边界。**本关只验证 CURRENT 现状可运行并冻结两组比较规则；Kernel 正式功能尚未开发，其完成验证在 G08，不要求此时提供新内核结果。**

本关承接 G00/G01/G02 遗留的“CURRENT 复现待确认”：核实实际记忆来源、提示词钩子触发与导出/加载方式；原始协议正文已保存不等于完整加载方式已验证。上述未知项不再反向阻塞之前的 Docker 检查、源码阅读或未计分环境冒烟，但解决前不接纳 CURRENT 正式结果、不放行 G04。

优先从 OpenDashboard 固定版本挑两类小任务。原生 Codex 默认为关闭的可选参照，不影响本关放行。

### Agent To-Do

- [ ] **G04.01** 冻结 M=1.4.188、K 三项起点、U、P、执行器/模型/推理/权限/工具；记录容器基础镜像版本、资源与启动方式、相对日常 Windows 的适配差异，未知项单列。
- [ ] **G04.02** 先解决前序遗留的 CURRENT 复现待确认，再在容器内忠实复现 M＋K：原始协议、通用经验、记忆提示词钩子及惯常操作。正文保存、显式加载协议、完整实际加载分开记录；不重写 2–5 条长期轨、同 Worker 返修或集成仅胶水规则，不用新 Kit 替代。无法保留真实机制时具体记录，再提 VM，不将技术冒烟当 CURRENT 正式结果。
- [ ] **G04.03** 选择 T1（边界清楚的可并行修改）、T2（公共契约／先后依赖），写目标、非目标、业务样例和最终验收。
- [ ] **G04.04** 共同验收对 CURRENT/KERNEL 一致且可理解；评分代码不由被验收 Agent 擅改。隐藏样例不增加隐藏需求。可选 CODEX 使用同一业务验收。
- [ ] **G04.05** 每轮使用独立业务目标 clone、固定 P 和实验分支；获准 push 时只推实验远端，不误推生产或上游主线。
- [ ] **G04.06** 对主两组统一实际模型、权限、网络工具、资源上限和人工帮助规则，记录原生子 Agent 设置；工作流本身不同正是比较对象，不强求相同计划。
- [ ] **G04.07** 事先规定环境失败、限流、超时、取消、人工救场和重跑规则；失败消耗保留。若 M/U 或运行方式不同，报告只能作整套方案比较。
- [ ] **G04.08** CURRENT 做一次预备冒烟，验证复现及共同验收；G02 已有 U 的技术冒烟，不再新增原生 Orca 组。预备结果不混入正式 12 次。
- [ ] **G04.09** 每轮采用独立提示词/记忆/钩子副本并允许 CURRENT 在本轮正常更新；结束后保留证据，下轮从经确认的起点重建，不自动带入本轮答案或记忆增量。通用经验保留范围、组间差异、导出/复位方法和实际更新权限写入同一规格；CODEX 不成为前提。
- [ ] **G04.10** 冻结胜利标准：只用 KERNEL 对 CURRENT 的净收益决定价值门；原生 Codex 即便后来测得更差，也不能代替新旧方案之间的改善。

### 一份规格就够了

```text
主对照：CURRENT vs KERNEL
CURRENT：M=Orca 1.4.188 及实际构建/运行方式；K=内置提示词＋长期记忆＋提示词钩子起点
KERNEL：U=上游起点；C=候选提交（G09 前填写并冻结）
M/U 是否同源同版本；若不同，具体差异与归因限制：
目标起点 P=；任务=T1/T2
目标、非目标、输入与允许 mock 范围：
共同验收及版本：
实际执行器/模型/推理/权限/可见工具：
总尝试/并发/时长/预算代理：
正常人工确认与额外救场定义：
环境失败、重跑和完整起点恢复方法：
三项配置来源/导出方式、保留通用经验、组间差异、本轮记忆更新权限及跨轮复位：
可选 CODEX：默认不执行；只有批准后另记条件与预算，不影响主实验。
```

费用不可得标未知；用现有可验证的次数、时长和并发约束控制执行。记录当前方案实际成本，不能为追求可比而把所有任务强制改用 Kernel 的编排方法。

### 验收与失败处理

CURRENT 忠实可运行、需求和共同验收明确、主两组的资源与起点策略已固定，才通过。CURRENT 无法复现就先修环境，不把它记为 Kernel 胜利。没有原生 Orca 性能结果或可选 CODEX 结果，**不构成阻塞**。

### 给 Agent 的指令

```text
只执行 G04。冻结当前 M/K、上游 U、目标 P、两类任务、共同验收与资源规则。
只复现当前实际方案并跑一次冒烟，不另设原生 Orca 比较组，也不顺手优化 CURRENT。
主实验预定 12 次 CURRENT/KERNEL；Kernel 完整闭环留在 G08 验证。
原生 Codex 只是以后可选的单列参照，默认不执行、不申请额外预算。
输出简短规格和真实结果；无法忠实复现的条件标明，提交待验收。
```

---

## G05｜实现受管模式、任务契约和计划检查

### 目标与入口

G04 通过后才改 Kernel 功能。此时环境和实验标准已经明确。新增内容先保持小而可测，不重构上游目录体系，不更换构建、状态库或执行器 SDK。

### 你要做

确认本版只支持本地 Git 目标仓库、首个执行器和明确的受管入口。初始并发建议按已有实验资源填写，不因“架构可扩展”自动增加。选择可用的已有设置/Run 入口承载开关，不审批新工作台。

### Agent To-Do

- [ ] **G05.01** 从 U 的运行调用链反向定位当前可用接入点。提交“上游已做/Kit 已做/需要补什么”三列表；没有必要性不新增模块。
- [ ] **G05.02** 实现【拟新增】按 Run 明确启用的受管模式；绑定仓库、规则版本和目标，不默认接管所有普通 Orca 操作。
- [ ] **G05.03** 关闭模式不追加 Kernel 提示词或额外放行逻辑。运行中关闭采用停止新派发→确认存活任务→人工接管，不静默解除保护。
- [ ] **G05.04** AI 提议计划，协调者/人确认，Worker 只接受已批准任务；Worker 修改本地文件不能给自己扩权。
- [ ] **G05.05** 最小任务契约包含目标/非目标、owner、允许写路径、依赖、起点与验收；关联 Orca 真实 Run/Task/Dispatch，不再造同名生命周期。
- [ ] **G05.06** 程序检查必需字段、无环依赖、并行写范围无交集、共享契约/锁文件/迁移/根配置有明确 owner。
- [ ] **G05.07** 路径表达先支持明确目录前缀和精确文件，目录边界按路径段比较。无法证明安全的 glob、链接、大小写或跨卷路径先拒绝/声明不支持，不用字符串包含猜安全。
- [ ] **G05.08** 允许小任务单 Agent、强耦合任务串行、计划调整；不检查“拆分数量相同”或强制所有任务都有三名 Worker。
- [ ] **G05.09** 规则唯一来源，启动摘要由已批准契约生成；完整规则可按需读取，但不把每份 diff 和全部会话广播给主控。
- [ ] **G05.10** 冻结本轮契约/验收版本；共享契约需要修改时暂停受影响任务并重新批准。首版不做在线学习式策略。
- [ ] **G05.11** 新字段向旧配置保持明确兼容；缺少 kernel 字段的普通 Run 仍按原逻辑工作。不要让新设置默认迁移所有历史任务。
- [ ] **G05.12** 先写正反例：合法计划、缺字段、环、父子目录重叠、同前缀不同目录、跨轨共享文件、单任务和串行。通过既有测试框架运行。

### 源码研究任务（不是照表强制改文件）

先读 `src/main/runtime/rpc/methods/orchestration-workers.ts`、`orchestration-dispatch-methods.ts` 以及它们的 schema、validation 和 topology 导入，再查状态存储的既有抽象。[S15–S17][S21]

重点找“实际副作用前共同经过的位置”。不要只在 renderer 按钮或 CLI 文字层校验。若当前插件 Host API 不具备完整拦截能力，就做窄范围源码二开，不预先假设安装一个插件就够。[S27]

【拟新增】纯规则函数可以独立测试，运行适配层只调用上游已有服务；具体目录由你在 U 上选择。改动记录必须说明原入口、增加的判断、普通模式如何保持不变。

### 契约草图（设计示例，不是上游 schema）

```json
{
  "schemaVersion": 1,
  "objective": "一个有限需求",
  "nonGoals": ["不做全站重构"],
  "baseCommit": "由运行时核对的实际起始提交",
  "tasks": [
    {
      "key": "T1",
      "owner": "adapter",
      "writePaths": ["src/adapter/"],
      "dependsOn": [],
      "acceptance": ["批准的验收项引用"],
      "escalateWhen": ["必须改共享契约", "无法获得依赖"]
    }
  ]
}
```

上述逻辑 key 只用于计划关联，映射到真实 Orca Task；不能产生第二份同义 Task 状态。代码从何处获得受信契约由现有存储/配置决定，Worker 可写副本只是任务材料，不是授权源。

### 产物、验收与退回

产物：最小功能提交、规则正反例、普通模式回归结果、实际修改范围。验收：合法计划可走到受管入口，非法计划不能靠自然语言“例外”放行；关闭无残留；不改变 CURRENT 的 M/K 基线，也不改写已记录的 U 冒烟事实。

失败返回原规则轨。拒绝全部计划不算安全；新增内容导致普通运行失败不能用“本版只支持 kernel”解释。

### 给 Agent 的指令

```text
只实现 G05。先定位 U 的共同入口和原有配置，提交最短接入判断。
AI 只提议计划，程序检查关键不变量；复用已有效的 Kit 检查，不重写运行时。
开关限受管 Run，关闭保持原生；路径支持宁窄不猜，单 Agent/串行合法。
不实现派发平台、行业包或进化；改动用小提交和实际测试证明。
提交 G05 待验收，列出仍未实现、需 G06/G07 接续的部分。
```

---

## G06｜受约束派发、必要交接、容量与停止

### 目标与入口

G05 通过，契约和核心字段已稳定。复用 Orca 的监督 Worker，Kernel 决定能否放行，不重新决定如何创建 PTY 和维护进程。

### Agent To-Do

- [ ] **G06.01** 将任务契约关联到真实 Task/Dispatch；在任何创建 Worktree/终端/派发副作用之前检查模式、权限、写域、依赖验收和容量。
- [ ] **G06.02** 同时审查高层 worker-start、低层 dispatch、复用 terminal、重试、远端 `--on` 等路径。首版未支持的受管路径明确拒绝或要求人工退出受管；普通原生模式不受无关限制。
- [ ] **G06.03** 默认新写轨使用 new-top-level 独立工作区。共享 current 只允许经过确认的同轨续做，不作为独立并行方案。[S16]
- [ ] **G06.04** 接收实际创建回执，核对工作目录、branch/起点、agentTerminal、Dispatch 与 setup 状态；不能只记录预期命令。
- [ ] **G06.05** 对需要依赖安装的任务，等实际 setup 成功或使用仓库明确的 wait-for-setup；ready 不代替安装就绪。scratch 跳过 setup 必须有理由。
- [ ] **G06.06** 用现有串行化/事务能力保护“检查容量→占用→派发”过程；两条同时到达的请求不得都看到最后一个空位。若上游已有保护则复用，不另建调度服务。
- [ ] **G06.07** 统计活跃执行单元与总尝试；启动中/停止中/状态未知的资源按保守原则处理。任务报告完成但进程未释放，不自动认为占用已经消失。
- [ ] **G06.08** 原生内部子 Agent 设置在 CURRENT/KERNEL 主对照中一致并纳入资源说明；v0.1 不开放自主无限递归。新 Run 或改 task key 不能绕过本轮受管配额。
- [ ] **G06.09** 复用 Orca 消息，限定任务相关联系对象和允许的请求；求助能到达协调者，不能以“减少交流”为由让依赖信息缺失。
- [ ] **G06.10** 信息请求可以只读回答；写权扩大、接口变更、验收变化需新批准。消息里的扩权理由不等于授权。
- [ ] **G06.11** 对重复启动请求复用上游操作身份。回执丢失先查询真实 request/Worker 状态；absent 或超时不直接证明没有启动。
- [ ] **G06.12** 停止开关先封住后续派发，再请求停止准确 Dispatch；明确已停止/仍运行/未知。停止不删 Worktree，不全局 kill Node/Electron。
- [ ] **G06.13** 同轨修复复用原成果；能明确复用原 Agent 就按上游监督规则转移下一任务。身份不明时不双发给旧、新两个终端。
- [ ] **G06.14** 端口、测试库、生成目录先用独立配置或串行；发现冲突时退回，不扩为通用资源租约工程。
- [ ] **G06.15** 检查设定重试/总时长的终止行为，区别“等待一次超时”与“本轮预算已耗尽”。停止不代表费用精确归零，记录最终可观测用量。

### 特别注意两种“有派发但不等价”的路径

上游低层 `dispatch --inject` 可以有任务来源记录，却不等于 `worker-start` 的受监督资源生命周期。不能随后假定 worker-stop 会回收它。v0.1 需要监督时优先 worker-start；已有 terminal 用明确的监督接入方式。[S14][S19]

`--on` 远端路径必须在受管检查范围内：不支持就拒绝，不能因为在 worker-start 的本地分支加了检查，就漏掉提前转发的分支。[S15]

### 验收与退回

至少跑：两名独立 Worker、超过容量请求、同时到达的容量竞争、依赖未验收、setup 未就绪、受管远端/低层路径、停止与未知状态。原生模式回归同时通过。

**通过不是只看命令长得一致，而是实际启动和资源结果受控。** 失败回 Orca 接入轨修复；不改上游身份验证来让命令“能调用”。

### 给 Agent 的指令

```text
只实现 G06。复用 Orca worker-start/读取/消息/停止，Kernel 只加准入和边界。
检查必须覆盖真实副作用前的高层、低层、复用和远端路径；未支持的受管入口拒绝。
证明并发竞争不超配、未知状态不双启动、setup 与依赖不被 ready 冒充。
修复返回原轨；不新建调度/数据库/PTY/消息系统，不伪造 --from 或能力。
提交实际两 Worker 试验和反例结果，保持 G07 的成果验收状态尚未实现时明确标注。
```

---

## G07｜实际候选验收、依赖代码落地、整合与最小记录

### 目标与入口

G06 通过。必须把“报告完成、候选接受、整合可交付”分开，保持原生 completed 的原义，不靠改名混淆现有生命周期。

### Agent To-Do

- [ ] **G07.01** 复用原生消息身份校验，建立最小候选验收事实，关联 Task/Dispatch、当前契约、实际候选与依赖版本。不要让文本中的 ID 成为认证依据。
- [ ] **G07.02** 从受管仓库和真实提交取得实际差异，不信任 Worker 自报文件清单；检查基线关系和工作区是否存在未纳入候选的修改。
- [ ] **G07.03** 同时处理新增、修改、删除、重命名及相关文件模式；路径规范化要覆盖 Windows 大小写、路径段、链接和仓库外跳转。无法证明时停止，不“默认没越界”。
- [ ] **G07.04** 用受信版本的验收策略运行检查。Worker 可以编写测试，但不能未经批准改验收规则、配置或必需测试后自证成功。
- [ ] **G07.05** 将测试结果绑定到候选与必要依赖。未运行、异常退出、超时、没有发现应有测试、证据缺失都不能显示通过。
- [ ] **G07.06** 确认“下游允许开始”和“依赖代码已经在下游起点中”同时成立。accepted 的提交必须通过明确的基线/整合步骤进入新工作区；仅有任务表状态不够。
- [ ] **G07.07** 对共享契约先建立并验收契约基线，后续消费者从其精确版本开始。多依赖时先构造受验收的组合起点；不从移动的 main 或当前 feature 自动猜。
- [ ] **G07.08** 优先保留 Git 可追溯合并关系；基础 ancestry 可以用 `git merge-base --is-ancestor` 验证。确需 squash/cherry-pick 时必须额外证明内容对应，不用“SHA 不同但应该一样”放行。
- [ ] **G07.09** 对提交历史也做必要范围检查：不只看最终净差异。越界写入后再还原可能留下已推送历史；首次 v0.1 可采用线性 Worker 提交、特殊 merge 返回人工，避免静默漏判。
- [ ] **G07.10** 在隔离整合工作区构造最终候选，执行累计契约/集成/业务验收。各分支分别通过仍可能组合失败。
- [ ] **G07.11** 冲突返回相关 owner；整合 Agent 只能处理已批准的组装范围，不能在别人目录大改后称为“纯合并”。
- [ ] **G07.12** 测试后重新核对候选与目标。目标移动或候选改变，重新验证或停止。最终受控晋级核对预期目标版本，避免检查后又合入其他内容。
- [ ] **G07.13** 相同 Dispatch/候选的重复报告不重复验收副作用和合入；旧尝试不能覆盖新尝试。处理重放优先复用上游交付身份，新增最少验收去重事实。
- [ ] **G07.14** 人工最终确认。例外接管单独记录，不把豁免后的结果记成自动门禁通过。
- [ ] **G07.15** 记录一次完整运行摘要：版本/目标/分工/候选/验收/最终提交、失败、人工、时间与未知用量；原始日志引用现有 Orca，不创建新的记录平台。
- [ ] **G07.16** 完成后按真实资源状态复用、retain 或 release。验收需要保留活 Worker 调试时明确 retain，不能无限遗留“已完成但仍占资源”的执行器。

### 依赖落地示例

假设契约任务产生已验收提交 DEP，两个消费者随后执行。正确流程是：DEP 通过→创建包含 DEP 的受控起点→两个 Worker 从该起点建立→核对实际 HEAD 确实包含 DEP。

错误流程是：DEP 在任务表显示 completed→两个新 Worker 仍从旧 origin/main 开始→各自“遵守契约”。这个问题必须进入回归。

检查一个预期依赖是否已成为实际起点祖先的参考命令：

```text
git -C 已确认的下游工作区 merge-base --is-ancestor 依赖提交SHA 实际起点SHA
```

退出码 0/1 分别表示祖先关系成立/不成立；其他错误需要单独处理。是否存在正确 ancestry 只是版本条件，不替代契约/业务验收。[S30]

### 验收规则自身的保护

验收代码可以存在目标仓库，但决定本轮“必须跑什么”的策略来自已批准版本/独立执行者。不要直接执行 Worker 提供的任意 shell 字符串；沿用可信测试入口与参数白名单。确定性测试也会执行候选代码，只能在已授权实验环境运行，不能在生产主机或持有生产密钥的环境跑。

不需要签名平台、复杂 Release Manifest 或外部证明系统；关联现有 Git 版本和执行日志足够。保留旧 Kit 有效规则，不为“统一语言”全量重写。

### 验收与退回

产物必须包含一个合法候选通过、一个越界候选拒绝、一个分支单独通过但整合失败样例，以及一个依赖已完成却未进下游起点的拒绝样例。

最终通过的是受测版本，不是某段聊天结论。失败返回原验收/开发轨；不得降低必要测试、删除断言或临时改允许路径后补写“原本合法”。

### 给 Agent 的指令

```text
只实现 G07。Worker 报告不等于验收，原生 completed 不自动满足受管依赖。
从实际提交检查范围；受信策略运行测试；下游起点必须实际包含已接受依赖。
组合后重新验收，目标/候选漂移就停或重验，冲突回 owner，最终晋级由人确认。
记录最小事实并关联已有日志，保持重复/过期报告幂等，不建证据平台。
提交正常、越界、语义冲突、依赖未落地四类真实证据后等待放行。
```

---

## G08｜完整正负回归，冻结进入实验的条件

### 目标与入口

G05–G07 最小功能连通。测试者检查的是候选代码的行为，不是重新设计更多功能。遵守上游 `AGENTS.md` 的工程约束，沿用当前 U 的脚本与测试体系。[S01][S02]

### Agent To-Do

- [ ] **G08.01** 对照 G04 的 T1/T2 跑真实开发闭环；简单任务另做单 Agent/串行正例。
- [ ] **G08.02** 按下表构造确定性负例，不必全部付费诱导模型；程序门禁、运行集成和真实模型场景分开记录。
- [ ] **G08.03** 对 Kernel 关闭模式复做 G02 的相关普通功能冒烟，检查新增回退；这是工程回归，不增加原生性能组，也不能把关闭 Kernel 当作 CURRENT 的替代。
- [ ] **G08.04** 关闭使用同一依赖目录的活跃 Electron 后，再按 U 跑 lint/类型/测试/构建；原生模块切换通过现有脚本，不并行重建。[S08]
- [ ] **G08.05** 检查新增代码没有改禁用原生校验、引入另一套 store、超大模块或不必要新依赖；共享文件只由指定 owner 合并。
- [ ] **G08.06** 修改 skill 指南时通过上游生成与校验命令更新派生资源，不手改多份生成副本。[S02]
- [ ] **G08.07** 对 G03 的身份与主目录边界再做回归，确保功能改造没有改变 CLI 绑定或污染 D。
- [ ] **G08.08** 检查敏感信息未进入 Git、截图、日志摘要、测试快照或 Actions artifacts；原始日志保留在实验私有目录。
- [ ] **G08.09** 将每项标为通过/失败/未测/不支持，写明环境和版本。未测的关键功能不能靠“以后补测”放行。
- [ ] **G08.10** 审查是否增加了用户维护负担：手工文件数、每次介入、报告阅读量。能自动关联就不要要求人工复制。

### 故障与正例清单

| ID | 构造/触发 | 期望行为 | 层次 |
|---|---|---|---|
| F01 | 合法计划和合法候选 | 正常放行，不能全拒绝 | 单元＋集成 |
| F02 | 单一小任务 | 允许单 Agent，不强制拆分 | 真实任务 |
| F03 | 缺目标/验收/owner | 派发前拒绝并解释缺项 | 单元 |
| F04 | 依赖成环 | 拒绝/退回调整 | 单元 |
| F05 | 目录父子写域重叠 | 不同时派发 | 单元＋集成 |
| F06 | src/api 与 src/api2 | 不因字符串前缀误拒合法范围 | 单元 |
| F07 | 两个请求争最后容量 | 只有允许数进入，另一个等待/拒绝 | 并发集成 |
| F08 | 越界文件未在自报清单中 | 实际提交检查拒绝 | 确定性集成 |
| F09 | rename/delete/大小写/链接异常 | 正确检查或明确不支持并停止 | 确定性集成 |
| F10 | 越界后还原，净 diff 干净 | 历史规则适用时仍识别；不承诺已扫描运行期全部操作 | Git 集成 |
| F11 | 改契约文件自行扩权 | 不改变受信授权 | 集成 |
| F12 | 删除必需测试/改弱验收 | 不自证通过 | 集成 |
| F13 | 测试没跑/没发现预期测试 | 不按成功接纳 | 集成 |
| F14 | 测试失败/超时/进程异常 | 失败或未知，不能绿色 | 集成 |
| F15 | setup 仍运行却 Agent ready | 需要依赖的任务不误判就绪 | 真实运行 |
| F16 | Worker 报告完成但未验收 | 下游受管启动不放行 | 集成 |
| F17 | 依赖 accepted 但下游无该代码 | 拒绝错误起点 | Git 集成 |
| F18 | 两分支单测过，接口组合错 | 整合失败，回 owner | 业务集成 |
| F19 | 测试后候选/目标移动 | 失效、重验或停止 | Git 集成 |
| F20 | 同一完成消息重放 | 不重复合并/解锁 | 生命周期集成 |
| F21 | 旧 Dispatch 晚到 | 不覆盖当前结果 | 生命周期集成 |
| F22 | 启动已发生但回执丢失 | 查询原请求/状态，不盲目双启动 | 运行集成 |
| F23 | 停止结果未知 | 不发同范围第二写入者 | 运行集成 |
| F24 | 通过低层/远端入口启动受管任务 | 同一检查或明确拒绝，不能静默漏管 | RPC 集成 |
| F25 | 关闭模式时仍有任务 | 停新派发、保留、明确接管 | 真实运行 |
| F26 | 重启后无法恢复验收中状态 | 标记需重验/人工，不假称已通过 | 运行集成 |
| F27 | 任务结束但 Worker 未释放 | 正确报告占用/明确 retain | 运行集成 |
| F28 | 简单正常原生操作，Kernel 关闭 | 无新增行为回退 | 原生回归 |

F09 若选择明确“不支持某路径类型”，需只限制受管模式并写清范围，不能让普通 Orca 全部失效。反例只在一次性目标中构造，绝不对日常 repo 做危险注入。

### 参考质量命令

在 U 确认脚本后，停止同 checkout 的活跃开发应用，依次运行 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build`；开发时可先用现有 scoped test 与 changed-code 检查缩短反馈，正式 Gate 仍覆盖实际改动与必需上游回归。

不要无差别 format 全仓库、改掉 lint 规则、更新大量无关依赖。运行脚本生成的变动应识别是否预期；“构建需要”不是顺手改锁文件的理由。[S01][S02]

### 验收与 Agent 指令

功能放行要求：核心正例和必需负例通过；没有未解释的新回退；M/K/U/验收/支持范围明确；可停止与接管。当前不支持的功能不纳入宣传。修复完成后相关测试重跑，不能复用旧候选证据。

```text
只执行 G08。用现有测试体系验证 F01–F28 和 T1/T2，列出真实结果。
只修最小已确认缺陷，退回原轨；不发起新的加固平台或全仓重构。
原生模式、隔离和停止回归必须有结果；未测不填通过。
最后给出“可进入 G09 / 阻塞”的证据判断，C 代码与实验规格暂不再变更。
```

---

## G09｜冻结候选，执行 CURRENT / KERNEL 两组主对照

### 入口与人工动作

G08 通过。你批准本批预算和停止条件；冻结当前 M/K、Kernel 上游 U 与候选 C、目标 P、共同验收和实际配置。正式期间不改候选，也不临时增强 CURRENT 或给 KERNEL 额外工具。

**最低主实验是 12 次。CODEX 可选参照不计入这 12 次，也不是 G09 的必做工作。**

### Agent To-Do

- [ ] **G09.01** 创建批次编号，记录 M/K/U/C/P 与配置摘要。确认 CURRENT 是现用方案的忠实复现；KERNEL 没有重复加载完整旧 Kit；主组只有这两个。
- [ ] **G09.02** 先做不计分的重置演练，证明新 profile 之外的真实 Agent home、会话、全局规则及目标副本也按 G03 恢复。
- [ ] **G09.03** 按下表轮换 CURRENT→KERNEL 与 KERNEL→CURRENT；两个任务各三对，共六对，先后次序各占三对。不让两组同时抢同一机器资源。
- [ ] **G09.04** 每次从干净环境、固定 P 和新会话开始；复用构建产物不能带入上一轮工作区、对话或答案。CURRENT 的真实入口按 M 核验，不套用 Kernel 的包装。
- [ ] **G09.05** 核对实际模型／推理／权限／网络工具，记录限流、代理、运行模式和 M/U 差异。存在差异时限定为整体方案比较，不能声称全部效果来自内核代码。
- [ ] **G09.06** 从本轮目标和验收确定后计端到端时间，包括规划、目标准备、Worker、验证和人工等待；一次性工具安装与适配成本单列。
- [ ] **G09.07** 记录人工类别、主动分钟和原因；正常确认与代码救场分开。不要用无记录的人工作业把 CURRENT 或 KERNEL 结果修成成功。
- [ ] **G09.08** 所有协调、Worker、整合和重试计入投入；不能取得精确费用标未知，不填零；日志缺失单列。
- [ ] **G09.09** 由独立验收者／程序验证实际最终产物，条件允许时隐藏组别做人工评审。模型自报不能直接充当评分。
- [ ] **G09.10** 保留失败、超时、取消和预定义环境无效记录。按事先规则重跑，原始尝试及消耗仍留存，不挑最好一次。
- [ ] **G09.11** 若必须改 C、M/K 或共同验收，结束本批并保存结论；新版本另开批，不把多个版本拼成同一组结果。
- [ ] **G09.12** 只用 KERNEL 相对 CURRENT 汇总主结论：逐任务报告 n/N、人工／时间中位数与范围、成本、失败和取舍。没有可选原生 Codex 成绩不影响本项完成。
- [ ] **G09.13** 按已授权范围决定是否做额外迁移核查或 CODEX 参照；默认不做。若做，单列样本、条件、额外预算和结果，不改主比较分母与放行标准。

### 12 次初始运行台账

| 顺序 | 任务 | 运行顺序 | 状态 | 结果／人工／资源 |
|---|---|---|---|---|
| 01–02 | T1 | CURRENT-r1 → KERNEL-r1 | 待执行 | — |
| 03–04 | T1 | KERNEL-r2 → CURRENT-r2 | 待执行 | — |
| 05–06 | T1 | CURRENT-r3 → KERNEL-r3 | 待执行 | — |
| 07–08 | T2 | KERNEL-r1 → CURRENT-r1 | 待执行 | — |
| 09–10 | T2 | CURRENT-r2 → KERNEL-r2 | 待执行 | — |
| 11–12 | T2 | KERNEL-r3 → CURRENT-r3 | 待执行 | — |

这是探索性样本，不是统计显著性保证。资源不足可以先调整规格并披露；不能看到某组失败后才删样本。主对照无需维护原生 Orca 的独立性能环境、题目轮次或报告列。

### 可选 CODEX 参照：直接运行原生 Codex，不再复制 Orca

目的仅是了解“直接用原生 Codex”与工作流工具的差别，不作为当前方案必须放弃或 Kernel 已经优越的证明。

默认状态是**未启用／不执行**。若你以后明确批准，只执行以下有限流程：

1. 在与主实验相同或已说明差异的环境中，准备独立目标 clone、新会话和干净 Codex 配置，使用同一 P、业务需求和外部验收。
2. 直接启动实际可用的原生 Codex，不通过 Orca，不加载现用 Kit／Kernel，不调用二者的提示词钩子或外置编排。项目固有业务规则仍正常提供。
3. 模型、推理、权限、原生子 Agent／工具设置与资源预算事先写明；保持原生正常能力，不为制造弱基线强制禁用某项原生功能，也不替它接上你的外部多 Agent Kit。
4. 若要与两类主任务一一对应，可另做 T1/T2 各 3 次，共 6 次；这是可选额外工作，不进入主实验 12 次。样本更少时如实说明。
5. 结果使用单独标题和分母。没有原生 Orca 的任务／Dispatch 记录是正常差异，用独立最终验收与实际用量记录即可；无法对应的内核专属指标标“不适用”，不填假零。
6. CODEX 结果不能覆盖 KERNEL 对 CURRENT 的结论。Kernel 只胜过原生 Codex、却不如当前方案时，不能据此宣布本次二开有价值。

本节不新增安装脚本或声称已核实的 Codex 参数；实际启动以已安装版本的帮助和正常使用方式为准。也不要求为它建立第三套 Orca profile、runtime 或监督链路。

### 结果口径

| 维度 | 一次完整需求的记录 |
|---|---|
| 最终结果 | 无额外救场通过／救场后通过／失败／超时／取消／预定义环境无效 |
| 正确接纳 | 已知无效成果误接纳数、合法候选误拒数，附测试集分母；不可观察项不填零 |
| 人工 | 确认、澄清、搬运、修代码各自分钟和次数 |
| 时间 | 端到端、人工等待、失败浪费；一次性环境成本单列 |
| 资源 | 全部 Agent 和重试的已知用量、费用与未知项 |
| 维护负担 | 配置人工、新增改动面、每轮需维护材料 |
| 可比性 | M/U、模型、运行方式与工具差异；只能整体归因的限制 |

主结果表只保留以下结构，不预留第三套原生 Orca 列：

| 任务 | 方案 | 完整尝试数 | 无救场通过 | 救场后通过 | 人工分钟 | 耗时 | 资源／未知 | 主要失败 |
|---|---|---|---|---|---|---|---|---|---|
| T1 | CURRENT | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| T1 | KERNEL | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| T2 | CURRENT | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| T2 | KERNEL | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |

先判断交付质量，再比较人工、时间和成本。KERNEL 更贵但少救场可以有价值；表现相当却更繁琐则应删减。重复执行、独立最终验收与干净环境的原则沿用 [S33]；本次两组和 12 次安排是规划修订，不是来源给出的实测结果。

### 通过标准与 Agent 指令

**G09 通过标准是主两组实验可信，不是 KERNEL 必须获胜。** CURRENT 复现失真、环境污染或未记录帮助时，保留原因并按规则处理。CODEX 未启用、没有结果或不可运行，都不阻塞此关。

```text
只执行 G09。冻结当前 M/K、Kernel 上游 U/候选 C、目标 P 与共同验收。
只跑表中的 12 次 CURRENT/KERNEL 主对照，轮换先后，保留全部失败与消耗。
不再运行独立原生 Orca 性能组，不升级或优化 CURRENT 来凑同一基线。
每轮核对真实环境、版本和配置；不可避免的差异作为归因限制报告。
原生 Codex 默认不跑，只有额外明确授权才做单列表外参照；它不是放行前提。
输出 KERNEL 相对 CURRENT 的真实取舍；需要改候选就结束本批，别混写成绩。
```

---

## G10｜评审、实验版发布范围与安全退出

### 目标与人工动作

G09 记录可信。你决定继续、收缩或停留在实验状态；是否公开 Fork、邀请试用和发布可执行文件分别决定，不将一个 Git tag 视为完整安装发布批准。

### Agent To-Do

- [ ] **G10.01** 汇总功能门：关键正负例、真实运行、原生回归、隔离与停止是否通过。
- [ ] **G10.02** 汇总价值门：KERNEL 对 CURRENT 是否存在重复可见的增益，代价是什么。M/U 差异须明确；CODEX 仅可选单列，不替代主结论，也不要求原生 Orca 性能结果。
- [ ] **G10.03** 列出已验证环境、执行器、仓库类型、并发、限制、未测与已知缺陷。不说任意 SDK/无限并发/绝对安全。
- [ ] **G10.04** 验证关闭 Kernel 后能用普通工具继续维护产物，代码和数据不依赖某段 Agent 会话。
- [ ] **G10.05** 准备源码版安装/启动/停止说明，固定版本。首版优先源码试用，不急做 installer。
- [ ] **G10.06** 若需要安装包，另行核查 productName/appId、数据目录、单实例锁、URI scheme、CLI 安装路径、自动更新源、签名与发布账号；原生配置包含 Orca 的现有身份，不可仅改窗口标题就发包。[S26]
- [ ] **G10.07** 检查公开差异、测试附件、日志、截图与 Actions 无秘密；保留版权/许可与来源声明。[S34]
- [ ] **G10.08** 明确回退：保留上一个源码/构建版本，但不要把迁移后的同一应用数据库直接交给旧程序。使用匹配的旧 profile 快照或新 profile，先备份当前状态。
- [ ] **G10.09** 停止与清理分开；确认资源属本次实验且无人使用后才清理，删除需列出准确对象并授权。不要清理 D 或全局 Codex 历史。
- [ ] **G10.10** 若放行，最多先邀请 1–2 位熟悉 Agent 的人，在非关键仓库试用，告知费用、数据流、限制、停止方式与支持渠道。
- [ ] **G10.11** 写唯一下一优先事项：修隔离/降误拒/减少接入成本/补评估之一；不同时开行业平台、进化和多执行器扩张。

### 决策表

| 事实 | 结论 |
|---|---|
| 功能不过，存在误接纳/串线 | 不发布，回到责任关卡修复 |
| 功能过，价值证据不足 | 可用实验版，有限试用，不宣称效率提升 |
| 功能过，KERNEL 对 CURRENT 有增益且代价可接受 | 进入小范围内测，保留真实条件 |
| KERNEL 与 CURRENT 相当但更繁琐 | 删除流程/代码，保留有价值检查 |
| 增益仅来自提示词，代码无明确价值 | 接受做小 Kit/插件，不为 Kernel 名字扩大工程 |

### 给 Agent 的指令

```text
只执行 G10。分开总结功能与价值，主结论只依据 G09 的 CURRENT/KERNEL 数据；CODEX 可选。
默认交付固定源码实验版，不触发 installer、更新或外部发布。
核查退出、版本/profile回退、秘密与来源声明；列清支持范围和唯一下一步。
无收益也必须如实写，不新增进化层来挽救指标；等待用户决定公开和内测。
```

---

## 附录 A｜Orca 操作速查：先判上下文，再执行命令

**Docker-first 使用约束：** 以下沿用原稿的命令形状，不是已验证的容器启动说明。实验命令须使用实际容器内的非 root 身份、HOME/profile 与该实例 CLI；Windows PowerShell 示例不能直接当作 Linux 容器命令，也不能指向 DW 日常 home。G01–G03 按固定 M/U 核对后再使用；G00 不创建容器或任务。

### A1. 外部控制终端与协调 Agent 终端不能混用身份

外部终端用已确认的 checkout CLI 与 profile 做状态核查。需要监督派发时，在同一实验 Orca 识别的协调 Agent 终端里执行；从其真实环境选择命令。WSL 使用实际 `ORCA_CLI_COMMAND`，不要将含参数的命令字符串交给任意 eval。

以下以 PowerShell 中已经确认的 `$Cli` 为例。Bash 等价形式为 `node "$CLI" ...`。这些是参数示例，不是保证固定 U 的所有可选项相同。先对照本地 skill 与 `--help`；禁止编造旧命令兼容关系。

```powershell
# 外部/协调终端都可用于核查；先绑定对应 profile。
node $Cli status --json
node $Cli repo list --json
node $Cli worktree ps --json
node $Cli terminal list --json
```

如果输出显示目标为远端、运行实例不符或图状态未就绪，不继续写操作。Linux 不直接运行未知 bare orca；Windows 不靠文件关联执行 .mjs。

### A2. 注册一次性目标与固定 Git 起点

人批准 scratch/实验 clone 后，通过现有 repo add 注册，返回的完整 repo selector 继续使用。检查实际默认基线，不能假定独立 Worktree 从当前 feature 分支建立。

```powershell
# $Target 必须是已经批准且存在的实验 Git 仓库。
node $Cli repo add --path $Target --json
# 从回执取得真实 repo id，不猜字段或复用另一实例的 ID。
# node $Cli repo show --repo 'id:真实repoId' --json
# node $Cli repo set-base-ref --repo 'id:真实repoId' --ref '已冻结的实验基线ref' --json
```

默认基线必须在该目标仓库中可解析并对应 P。`--no-parent` 控制 Orca 关系，不控制 Git 基线；不把它当版本隔离。[S09]

需要实验远端时另建批准的测试远端/命名空间。不要向公开产品仓库主线直接推送负例。首次 commit 用实验用户/仓库明确的 Git 身份，不能更改日常全局身份来通过提交。

### A3. 监督任务生命周期（在合法协调终端执行）

```powershell
node $Cli orchestration run-create --objective '明确要求监督、等待结果并验收的有限任务' --json
node $Cli orchestration task-create --spec '已确认的任务说明，含范围、验收与求助条件' --json
# 将真实回执的 Task ID 放入 $TaskId：
node $Cli orchestration worker-start --task $TaskId --worktree new-top-level --name 'approved-track' --agent codex --setup run --json
node $Cli orchestration task-list --json
node $Cli orchestration dispatch-show --task $TaskId --json
```

只在真正需要依赖安装时使用 setup run；先审查它会做什么。指定模型/effort时使用 U 已支持的 worker-start 参数，记录实际 launch，而不是把不支持的参数塞进 worktree create。

协调者不能复制另一终端的能力来调用。本文模板里不会提供认证 token；实际 Worker 要按 Orca 注入的当前 Task/Dispatch 回报。legacy/恢复消息出现时服从其精确上下文，不混用当前命令冒充旧权限。

### A4. 看进度时用只读方式，不抢走协调者的信箱

```powershell
node $Cli orchestration task-list --brief --json
node $Cli orchestration worker-show --dispatch $DispatchId --json
node $Cli orchestration worker-read --dispatch $DispatchId --limit 50 --json
node $Cli orchestration check --peek --format --json
```

`worker-read` 根据可证明的会话返回内容，按回执的来源和 cursor 继续；来源改变时按提示新读，不猜日志路径。不需要全量视觉布局就不请求它。监控者只用 peek；只有负责处理整批消息的协调者做消费与 ACK。[S07][S14][S22][S35]

### A5. 等待、回答与 ACK

```powershell
# 示例等待窗口，不是“到时就杀死 Worker”的任务时限。
node $Cli orchestration check --wait --types worker_done,escalation,question --timeout-ms 60000 --json
# 一批内有问题时：
node $Cli orchestration reply --id $MessageId --body '在已批准边界内的回答' --json
# 整批每条消息都已处理/可靠登记后，才确认真实 Delivery ID：
node $Cli orchestration check --ack $DeliveryId --json
```

同一 Delivery 未确认会重放。持久化必要处理结果/验收待办后再 ACK；首版重启时无法证明验收完成，可以标“需重验”，不能默默当已通过。类型过滤不是允许忽略同批其他消息。

stdout 是 JSON，等待心跳可能在 stderr。解析 JSON 时不要使用 `2>&1` 合并后送解析器；可以分文件保存两个流。等待超时或空批次是检查点，不是失败证明。按照本轮预算和实际状态继续等待，避免每秒循环或无限等待。

### A6. 完成后复用、保留与释放

对已结算 Worker，选择唯一后续动作：同轨马上继续就按真实 terminal handle 监督复用；要调试就 retain 并记录；否则 release。不要对状态未知或尚未完成的 Worker 做“完成清理”。

```powershell
node $Cli orchestration worker-show --dispatch $DispatchId --json
# 仅当已经完成且无需继续：
node $Cli orchestration worker-release --dispatch $DispatchId --json
# 仅当确有调试保留需求：
# node $Cli orchestration worker-retain --dispatch $DispatchId --json
```

保留意味着可能仍占运行资源，不能从配额账上假消失。release 对不属于其监督范围的资源可能只报告保留，必须读回执。[S19]

### A7. 停止与结果不明的恢复

```powershell
node $Cli orchestration worker-show --dispatch $DispatchId --json
# 用户停止或达到已批准边界时，操作准确对象：
node $Cli orchestration worker-stop --dispatch $DispatchId --json
node $Cli orchestration worker-show --dispatch $DispatchId --json
```

若创建响应丢失，先按原回执给出的 request ID查询，存在精确 retry 指令才复用同一操作身份；没有回执时核对已存在资源，不猜“什么都没发生”。

只有明确已失败/已停止且资源边界清楚，才申请 retry。`abandon` 不等于停止进程；`reset --all` 不等于修复；全局 killall/pkill/node taskkill 不是恢复方案。

### A8. 不再使用或不应误用的行为

| 行为 | 本手册处理 |
|---|---|
| 旧 coordinator-start / run-stop 等旧调度命令 | 按 U 的 migration/help 处理，不假定兼容 |
| `worker-start --worktree current` 作为独立并行轨 | 不采用；current 共享 checkout |
| 低层 dispatch 当作自动获得进程监督 | 不采用；核查监督资源类型 |
| 人工修改 Task completed 绕过验收 | 不成为受管 accepted 的依据 |
| 将所有 worker_done 自动释放后才决定验收 | 先保留必要候选与输出，按真实状态处理；需要活会话则显式 retain |
| 用 terminal handle 作为永恒身份 | 不采用；任务/Dispatch 是关联依据，handle 只用于有效路由 |
| 每秒轮询并反复重新派发 | 用原生等待/只读查询与有界预算 |

---

## 附录 B｜Docker、独立用户与虚拟机的实操准入

### B1. 容器非 root 身份与 Windows 保护

- [ ] 不创建独立 Windows 用户；记录日常 D 的当前身份和保护范围。
- [ ] G00 放行后，容器运行时明确非 root UID、真实 HOME、Codex home、Orca profile；不能只修改 HOME 文本冒充身份隔离。
- [ ] 源码、目标、记忆和必要配置均为容器内独立副本；只导出经确认的基线项目，不复制整个日常目录。
- [ ] Windows 稳定 Orca 用于现有协调；未经验证的 Kernel 不作为唯一开发/恢复工具。实验协调和 Worker 身份必须来自容器自身运行实例。
- [ ] G03 验证链接、挂载、回写与跨轮复位；非 root 标志本身不证明隔离。

### B2. 虚拟机备选（未启用，不与 Docker 并建）

- [ ] 确认宿主支持、可用资源、工具来源和许可，不盲目安装新的大型虚拟化栈。
- [ ] 建立专用实验系统，更新必要运行环境；以非管理员用户执行开发。
- [ ] 只开放完成任务需要的网络，禁止自动挂入整个 HOME/生产盘/SSH agent；核查共享剪贴板与文件夹。
- [ ] 安装必要工具；按真实 M 准备 CURRENT 程序，按 U/C 准备 Kernel 构建，不新增原生性能版本；不导入解答或运行历史。
- [ ] 完成必要身份认证后，创建私有干净起点快照；认证状态可能变化，恢复后仍需核对实际可用性，不将异常自动算成模型失败。
- [ ] 每轮恢复同一起点，使用相同资源，应用本组配置，导入目标 P，运行并导出结果。
- [ ] 对照结束前不删除快照；需要删除时先确认代码、结果和备份已导出且去敏。

快照提供可恢复实验起点，不意味着所有服务端记忆、账号限流或外部缓存都被复位。外部状态需要在结果里说明。

### B3. Docker-first 主路线准入

**本次不提供声称已经验证的 Dockerfile。** 官方 headless 路线可以参考，但你的固定 U、操作系统和认证方式仍需独立验证。不能为了“容器里启动失败”直接使用 `--privileged`、挂载 Docker socket 或默认 `--no-sandbox`。[S25][S31]

- [ ] 已确定目的：CURRENT/KERNEL 两组的实验 Orca、Codex、HOME、记忆副本和目标均在相同容器运行方式下验证；初期一套 Orca 及其 Worker 在一个实验容器内，复用 Worktree。
- [ ] 固定基础镜像与工具版本，记录 CPU架构、资源限额和构建产物；不使用漂移 latest 作为正式实验唯一依据。
- [ ] 按 U 的 headless 文档准备 Electron系统依赖、显示支持和 AppImage 解包；不假设普通 node 镜像即可启动。
- [ ] 用非 root 身份运行，控制文件所有权与资源；可写目录只留给 profile、目标和必要缓存。
- [ ] 目标只挂本轮副本；工具源码可只读挂载时优先只读。bind mount 默认可写，必须逐个审查。[S29]
- [ ] 不挂日常 .codex、整个 HOME、全部 SSH 密钥、宿主根目录或 Docker daemon socket。
- [ ] 端口明确绑定本机，例如发布语法使用 `127.0.0.1:宿主端口:容器端口`；端口号取实际配置。默认发布不应被误认为只本机访问。[S31]
- [ ] Orca `--pairing-address` 是对外通告地址，不是“只监听 localhost”的保证；核查实际监听和映射。配对 URL/认证串不公开。[S25]
- [ ] 先执行与 G02/G03 相同的启动、监督、停止、配置与会话隔离试验，再进行性能比较。
- [ ] CURRENT/KERNEL 同容器方式、同资源与依赖；CURRENT 若无法忠实复现，不为使用 Docker 偷换其实际工作方式。容器只回收自身不代表外部 volume 干净；每轮数据卷起点也要固定。

不做宿主无头与桌面混合计时。本轮已选择这条路线，只调整 G00–G04 的运行环境验证，不把 kernel 变成新的容器编排平台。

---

## 附录 C｜源码探究地图：AI 应该从哪里看起

原则：先按调用链找现成能力，再确定最小修改；不凭表中名称断言每个版本都有同样接口。这里是本次见到的路径/职责，固定 U 后要复查引用、调用者和现有测试。

| 关卡 | 上游阅读入口 | 需要回答的问题 |
|---|---|---|
| G01/G02 | `AGENTS.md`、`.github/CONTRIBUTING.md`、`package.json` | 构建、测试、平台规则、后台运行、生成文件如何维护？ |
| G02/G03 | `src/main/startup/configure-process.ts` | dev userData何时设置？何时规范 ORCA_USER_DATA_PATH？ |
| G02/G03 | `config/scripts/run-electron-vite-dev.mjs` | 环境继承、标签、调试端点、实例如何启动？ |
| G02/G03 | `orca-dev.mjs`、`dev-cli-terminal-wrapper.mjs`、`install-dev-cli.mjs` | CLI绑定在哪里？查询有无文件写入？全局链接会不会串线？ |
| G03 | `src/cli/runtime/metadata.ts`、`status.ts`、`src/shared/runtime-bootstrap.ts` | 端点和真实状态如何确认？哪里含秘密？ |
| G03 | `src/main/codex/codex-home-paths.ts` | managed/system home从何而来？全局资源是否链接/复制？ |
| G03 | `codex-config-mirror.ts`、`codex-session-bridge.ts`、`codex-session-backfill.ts` | 有无双向设置或会话传播？真实launch使用哪条路径？ |
| G02/G06 | `orca.yaml`、`run-internal-dev-setup.mjs`、`windows-setup-shell.md` | worktree创建时会执行什么？shell实际是哪一种？ |
| G05/G06 | `orchestration-workers.ts`、`orchestration-worker-start-validation.ts` | 身份/就绪/分支/远端/副作用前检查在哪里？ |
| G05/G06 | `orchestration-worker-start-schema.ts`、`orchestration-worker-topology.ts` | 参数与工作区语义是什么？如何避免共享current？ |
| G06 | `orchestration-worker-setup-gate.ts`、receipt相关模块 | ready、setup、失败残留怎样表达？ |
| G06/G07 | `orchestration-dispatch-methods.ts`、消息发送路径 | 低层路径能否跳过检查？来源证明是否保持？ |
| G07/G08 | `lifecycle-reconciliation.ts`、`orchestration-check-methods.ts` | completed、重放、ACK与候选验收怎样区分？ |
| G06/G08 | `orchestration-worker-control.ts`、stop/release模块 | 实际live/unknown如何核对？谁有清理权限？ |
| G05 | `src/shared/plugins/plugin-host-api.ts` | 插件是否真的暴露所需能力？不够时最小fork是什么？ |
| G08 | `ensure-native-runtime.mjs`、现有相关测试 | Node/Electron依赖切换和并发构建风险如何避免？ |
| G10 | `config/electron-builder.config.cjs`、发布workflow | 包身份、协议、更新与分发会不会覆盖D？ |

### 本地定位命令（只读，避免全仓无差别输出）

```text
git grep -n 'ORCA_DEV_USER_DATA_PATH' -- src config
git grep -n 'getSystemCodexHomePath' -- src/main
git grep -n 'syncSystemCodexResourcesIntoManagedHome' -- src/main
git grep -n 'worker-start' -- src/cli src/main/runtime
git grep -n 'worker_report' -- src/main/runtime
```

由函数调用者继续追踪实际生效路径。发现 deprecated/legacy函数时必须确认它是否仍用于当前配置；只看到函数存在不能声称全部运行都经过它。

Windows 新进程调用沿用仓库共享封装，WSL 使用既有参数构造；不要为了本机快速成功引入无条件 shell拼接、硬编码斜杠或改全局环境。上游明确有相关工程规则。[S01]

---

## 附录 D｜常见失败：诊断顺序与禁止修复

| 现象 | 先查什么 | 合理下一步 | 不要做什么 |
|---|---|---|---|
| CLI查询到日常版 | profile、checkout、全局alias、继承远端选择 | 显式重绑定并复验 | 改个窗口名就继续 |
| status ok:true但不可达 | result.runtime与实际进程 | 等就绪或查启动失败 | 当作运行成功派发 |
| 切换 profile 后原先 CLI 路由变了 | 同 checkout 的 out/bin 重写 | 串行或独立 checkout，核验真实实例 | 同目录同时开多个 profile |
| CURRENT 混入 Kernel 指令，或可选 CODEX 加载 Kit | system .codex 资源、用户规则与钩子 | 核验容器独立 HOME/副本与组别配置，失败后提议 VM | 删除日常规则，或清掉 CURRENT 本应有的 Kit |
| 新profile看到旧会话 | 会话桥接/回填/真实home | 完整干净起点恢复 | 只清聊天窗口 |
| 修改实验模型设置影响别处 | 配置回写调用链 | 修复容器回写范围后复验 | 声称CODEX_HOME万能隔离 |
| 原生模块ABI错误 | Node/Electron构建切换 | 关闭同checkout运行，按现有脚本准备 | 一直重装全部工具 |
| pnpm安装改了lockfile | pnpm版本、U、脚本 | 保留差异，查原因 | 静默提交新锁文件 |
| Windows setup失败 | shebang/cmd/Git Bash/WSL实际shell | 按上游shell规则修配置 | 假定默认终端就是setup shell |
| Worker ready但依赖未装好 | setup receipt / wait-for-setup | 等真正完成或设置正确策略 | 让Agent反复猜包缺失 |
| worker-start无协调者权限 | 终端是否真实Agent/绑定Run | 在合法协调终端执行 | 复制token或伪造--from |
| 旧命令不认识 | U技能指南、help、迁移说明 | 最小兼容修复并记录 | 自建旧调度器 |
| worker_done已出现但不该合入 | 实际候选、测试、授权 | 走独立验收 | 手动改completed冒充合格 |
| 子任务不含依赖代码 | 实际Git起点与SHA | 正确建立组合基线 | 只重发一段接口说明 |
| 消息重复出现 | Delivery是否正确ACK、结果是否已登记 | 幂等处理整批并确认 | reset --all |
| JSON解析Extra data | 是否合并stderr heartbeat | 分开两条流 | 删除心跳或忽略解析错误 |
| 一次wait超时 | Worker真实状态、预算剩余 | 有界继续等待 | 立即kill/retry |
| 启动响应丢失 | request/dispatch/实际资源 | 条件恢复原操作身份 | 盲目再启动同范围Worker |
| stopped后还有终端 | setup/预存/非监督资源所有权 | 逐项说明，授权后处理 | 全局killall/node taskkill |
| 旧候选比新候选晚报告 | Dispatch有效性与候选版本 | 拒绝过期结果 | 覆盖新验收 |
| Git正常合并但界面坏了 | 共同契约/E2E/数据语义 | 退回owner修复 | 仅看Git无冲突 |
| 所有负例被拒但没交付 | 正常样例误拒、规则过度 | 降误拒，保留正例 | 以零误接纳宣布成功 |
| Docker里文件改了宿主 | bind mounts及写权限 | 缩小挂载/只读工具源 | 挂整块磁盘方便调试 |
| 配对地址是localhost仍对外开放 | 实际listen与端口publish | 本机绑定/防火墙核验 | 以通告地址当访问控制 |
| 回退旧版本打不开数据 | schema迁移与profile版本 | 匹配快照或新profile | 覆盖/强行降级原数据 |
| API限流导致一组慢 | 账户其他任务/时间顺序/资源 | 按规格记录与轮换 | 删除该组慢结果 |

Windows setup实际选择不一定服从交互终端偏好，需按上游说明确认。[S37] 其他诊断以源码地图和各关真实证据为准。

---

## 附录 E｜最少记录、上下文恢复与变更规则

### E1. 每关交接只填这七行

```text
关卡：Gxx；状态：待验收/阻塞
实际范围：这次做了什么，不超过三句
版本：M/K（当前方案）、U/候选SHA（Kernel）；未提交或不可得如实写明
执行证据：命令、退出状态、对应日志位置
结果：通过、失败、未测分别列明
人工/风险：需要你确认什么，未解决什么
请求：批准本关；或继续原关修复（不自行跳下一关）
```

“测试通过”必须有实际检查记录；“已push”必须有对应远端提交，不用分支名代替固定候选。记录经过脱敏，原始秘密只保留在授权私有位置。

### E2. 开发 Agent 失去上下文后怎么办

先读本手册看板，确认当前关卡与批准状态；再查实际工作区/分支/提交和未提交改动，阅读最近交接。事实不符就报告差异，不能 reset 成自己熟悉的起点。身份/Worker状态不明先只读核查，不创建替代写入者。

不同 Agent 的会话历史不需要无损迁移。交接靠目标、边界、版本和实际成果，而不是把所有原始对话重新灌给主控。

### E3. 哪些变化使旧通过记录失效

| 变更 | 至少重新检查 |
|---|---|
| 更换上游U、运行OS/宿主模式、Agent主目录策略 | G02–G04；下游受影响回归与实验 |
| 更换 M/K 实际内容 | 重验 CURRENT 复现与比较规格；旧 CURRENT 成绩单列，不混批 |
| 修改CLI包装、启动环境、Codex配置路径 | G03及相关Worker回归 |
| 修改契约、范围、验收或依赖起点 | 受影响G05–G08；正式实验开新批 |
| 修复候选 C | 相关回归；旧正式成绩不与新候选合并 |
| 改实际模型/推理/权限或新增工具 | 实验规格更新，批次可比性重新说明 |
| 打包安装或改变更新来源 | G10分发检查，不沿用源码隔离结论 |

已知错误修复可以继续原关；重大范围变化要由你决定，不要求每次小提交重新讨论整个路线图。

### E4. 禁止自动执行的高风险“捷径”

不覆盖 D；不全局升级/改PATH；不 `git reset --hard` / `git clean -fd` 清未知工作；不强推/镜像推送；不输出凭证；不伪造协调者；不把生产盘挂给容器；不因测试失败关闭全部权限；不删除验收；不把运行中的SQLite普通复制当已验证备份；不把生成此文档算作开发完成。

必要的破坏性操作必须列出准确对象、影响、备份和回退方式，经明确批准，只对实验范围执行。

### E5. 现在交给 Agent 的总启动指令

```text
这是 Orca-Kernel v0.1 的唯一执行手册。你是协调/执行 Agent。
先读0–2节与看板，再只执行当前获批关卡；当前初始为G00，其他全部待开始。
这是需要监督、监控、等待结果、验收并逐关推进的任务，不是交出后结束的handoff。
只复用Orca已有运行设施，把新增限制在契约、准入、验收和最小记录。
主比较只有CURRENT（当前实际M/K）与KERNEL，共12次；不要重建原生Orca性能组。
上游U仅做技术冒烟和回归。原生Codex默认不执行，可选单列且不阻塞放行。
所有状态以实际证据为准；不能编造环境、ID、测试、提交或对照结果。
未经放行不进入下一关；失败在原关原轨修复，不创建新的庞大加固流程。
先只读盘点 Docker/WSL、日常环境及 CURRENT 三项来源；本轮已选 Docker-first，VM 仅备选，不创建 Windows 用户或 Kit 系统；不要安装或启动收费任务。
每次按E1提交短交接，等待用户确认。
```

---

## 附录 F｜来源、版本与研究边界

### F1. 本次证据边界

原稿 1.0 已读取《逐步验收清单与隔离方案》，并检索上游、官方指南及关键实现，升级了 Agent home 隔离要求。本次 1.1 按用户要求只调整为 CURRENT/KERNEL 两组主对照和可选原生 Codex 参照，并同步命令、环境和台账；没有重新进行在线研究。以下来源和技术判断沿用原稿，不当作本次新实测。

原稿检索见到的上游参考提交为 `b107f42c4c39bd025157f32ecc9b32c2edeb25dc`。对以下四个关键文件额外打开了该完整 SHA 下的版本：`configure-process.ts`、`codex-home-paths.ts`、`dev-cli-terminal-wrapper.mjs`、`skill-guides/orchestration.md`。其余来源主要是检索时 main，可能变化。**该 SHA 仅是本次阅读锚点，不是已经为你验证适合 K 的 U，也不宣称所有源码已在这个 SHA 完成审计。**

没有运行本机 Orca、实际 Kit、本文命令、本次 12 次主比较或可选 CODEX 参照；没有创建外部 Fork。所有待办仍未执行。G01 固定 M/K/U 后必须核对各自命令与调用链；源码事实不等于主机隔离实测。本次修订完成也不增加关卡通过数。

### F2. 来源索引

[S01] Orca 工程约束与贡献指南：
- https://raw.githubusercontent.com/stablyai/orca/main/AGENTS.md
- https://raw.githubusercontent.com/stablyai/orca/main/.github/CONTRIBUTING.md

[S02] 构建/测试/依赖与生成资源脚本：
- https://raw.githubusercontent.com/stablyai/orca/main/package.json

[S03] dev userData 与规范化环境：
- https://raw.githubusercontent.com/stablyai/orca/b107f42c4c39bd025157f32ecc9b32c2edeb25dc/src/main/startup/configure-process.ts

[S04] 显式开发 CLI 包装：
- https://raw.githubusercontent.com/stablyai/orca/main/config/scripts/orca-dev.mjs

[S05] checkout/profile 绑定的包装文件：
- https://raw.githubusercontent.com/stablyai/orca/b107f42c4c39bd025157f32ecc9b32c2edeb25dc/config/scripts/dev-cli-terminal-wrapper.mjs
- https://raw.githubusercontent.com/stablyai/orca/main/config/scripts/run-electron-vite-dev.mjs

[S06] 开发 CLI 全局别名副作用：
- https://raw.githubusercontent.com/stablyai/orca/main/config/scripts/install-dev-cli.mjs

[S07] CLI元数据、状态与本地认证：
- https://raw.githubusercontent.com/stablyai/orca/main/src/cli/runtime/metadata.ts
- https://raw.githubusercontent.com/stablyai/orca/main/src/cli/runtime/status.ts
- https://raw.githubusercontent.com/stablyai/orca/main/src/shared/runtime-bootstrap.ts

[S08] Node/Electron 原生模块准备：
- https://raw.githubusercontent.com/stablyai/orca/main/config/scripts/ensure-native-runtime.mjs

[S09] Orca CLI、平台选择、Worktree与基线：
- https://raw.githubusercontent.com/stablyai/orca/main/skill-guides/orca-cli.md

[S10] Codex system/managed home与全局资源：
- https://raw.githubusercontent.com/stablyai/orca/b107f42c4c39bd025157f32ecc9b32c2edeb25dc/src/main/codex/codex-home-paths.ts

[S11] Codex配置镜像与设置回写：
- https://raw.githubusercontent.com/stablyai/orca/main/src/main/codex/codex-config-mirror.ts

[S12] Codex会话桥接：
- https://raw.githubusercontent.com/stablyai/orca/main/src/main/codex/codex-session-bridge.ts

[S13] Codex会话回填：
- https://raw.githubusercontent.com/stablyai/orca/main/src/main/codex/codex-session-backfill.ts

[S14] Orca监督编排与消息指南：
- https://raw.githubusercontent.com/stablyai/orca/b107f42c4c39bd025157f32ecc9b32c2edeb25dc/skill-guides/orchestration.md

[S15] Worker启动主调用链与前置验证：
- https://raw.githubusercontent.com/stablyai/orca/main/src/main/runtime/rpc/methods/orchestration-workers.ts
- https://raw.githubusercontent.com/stablyai/orca/main/src/main/runtime/rpc/methods/orchestration-worker-start-validation.ts

[S16] 工作区拓扑与启动放置：
- https://raw.githubusercontent.com/stablyai/orca/main/src/main/runtime/rpc/methods/orchestration-worker-topology.ts

[S17] Worker参数契约：
- https://raw.githubusercontent.com/stablyai/orca/main/src/main/runtime/rpc/methods/orchestration-worker-start-schema.ts

[S18] setup闸门：
- https://raw.githubusercontent.com/stablyai/orca/main/src/main/runtime/rpc/methods/orchestration-worker-setup-gate.ts

[S19] 停止、释放与保留的边界：
- https://raw.githubusercontent.com/stablyai/orca/main/src/main/runtime/rpc/methods/orchestration-worker-stop.ts
- https://raw.githubusercontent.com/stablyai/orca/main/src/main/runtime/rpc/methods/orchestration-worker-release.ts

[S20] 生命周期与worker_report：
- https://raw.githubusercontent.com/stablyai/orca/main/src/main/runtime/orchestration/lifecycle-reconciliation.ts

[S21] 低层派发入口：
- https://raw.githubusercontent.com/stablyai/orca/main/src/main/runtime/rpc/methods/orchestration-dispatch-methods.ts

[S22] Worker状态与读取：
- https://raw.githubusercontent.com/stablyai/orca/main/src/main/runtime/rpc/methods/orchestration-worker-control.ts

[S23] Orca仓库setup：
- https://raw.githubusercontent.com/stablyai/orca/main/orca.yaml

[S24] 可选内部开发setup执行：
- https://raw.githubusercontent.com/stablyai/orca/main/config/scripts/run-internal-dev-setup.mjs

[S25] 官方headless Linux/Docker与服务暴露说明：
- https://raw.githubusercontent.com/stablyai/orca/main/docs/reference/headless-linux-server.md
- 本轮已读取的固定版本参考：https://raw.githubusercontent.com/stablyai/orca/v1.4.188/docs/reference/headless-linux-server.md 。它不是运行验收；其中 latest、root 或对外服务示例不能替代本轮固定版本、非 root、sandbox 与 loopback 限制。

[S26] 打包身份与发布配置：
- https://raw.githubusercontent.com/stablyai/orca/main/config/electron-builder.config.cjs

[S27] 插件Host API边界：
- https://raw.githubusercontent.com/stablyai/orca/main/src/shared/plugins/plugin-host-api.ts

[S28] GitHub Fork与workflow控制：
- https://docs.github.com/en/pull-requests/how-tos/work-with-forks/fork-a-repo
- https://docs.github.com/en/actions/how-tos/manage-workflow-runs/disable-and-enable-workflows

[S29] Docker bind mounts默认写权限：
- https://docs.docker.com/engine/storage/bind-mounts/

[S30] Git版本关系与Worktree：
- https://git-scm.com/docs/git-merge-base
- https://git-scm.com/docs/git-worktree

[S31] Docker安全与端口发布：
- https://docs.docker.com/engine/security/
- https://docs.docker.com/engine/network/port-publishing/

[S32] WSL配置、自动挂载与互操作：
- https://learn.microsoft.com/en-us/windows/wsl/wsl-config

[S33] Agent评估方法：
- https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents

[S34] Orca根许可证：
- https://raw.githubusercontent.com/stablyai/orca/main/LICENSE

[S35] 消息check实现：
- https://raw.githubusercontent.com/stablyai/orca/main/src/main/runtime/rpc/methods/orchestration-check-methods.ts

[S36] Windows账户创建与类型管理：
- https://support.microsoft.com/en-us/windows/security/identity-signin/manage-user-accounts-in-windows

[S37] Windows setup shell语义：
- https://raw.githubusercontent.com/stablyai/orca/main/docs/reference/windows-setup-shell.md

[S38] Codex官方配置说明（仅说明Codex自身，不替代Orca调用链验证）：
- https://developers.openai.com/codex/config-advanced/

**最终执行原则：保护日常环境，忠实复现当前方案；只做 CURRENT/KERNEL 两组主对照，原生 Codex 可选；先取得真实证据，再推进下一关。**
